# 11 · 本地化收敛施工单（M5/M6 → V1）

> 状态复核：2026-09-13；核对的 `main` 源码为 `eef716cd69b84e0676eb90ad3ee776397aa6b515`。
> **这是当前唯一有效的施工任务清单**，替代旧 10 章及 M5/M6 handoff 的任务安排。
> R0 环境/采集条件已落实；R1/R2 已在 `d3fd6fdc` 归档批次验收；当前继续 R3–R6。
> 最新 `eef716cd` 只有 scoped GUI 回归及部分工程记录，不能继承上一批次的完整 PASS。
> 本次为文档、关键源码和归档日志复核；复核环境是未安装 Cargo 的 Linux，未重跑 macOS 构建、实机或网络验收。

## 目录

- [1. 目标、范围与现状](#scope)
- [2. 已定边界](#constraints)
- [3. 施工批次 R0–R6](#batches)
- [4. 必跑验证](#verification)
- [5. 验收条件 C1–C10](#acceptance)
- [6. 证据与交付](#delivery)
- [7. Goal 指令](#goal)

<a id="scope"></a>
## 1. 目标、范围与现状

在已恢复本地运行时的基础上完成残留清理和最终验收：GUI/TUI 可构建运行，本地功能和旧数据
不丢，Warp 云控制面及依赖消失，通过工程、运行时和 macOS 验收。

本批包含 M5/M6 的剩余实现，以及 V0 遗留的严格验收；**不包含 M7 品牌发布、首个 tag、
完整 MIT 重实现或新的本地 Agent 协议**。运行时 Term4u 身份保持不变。

### 1.1 已完成或已有批次证据：不重复开发

| 项目 | 已有实现/证据 | 后续动作 |
|---|---|---|
| blocklist 路线 | [决策记录](baseline/blocklist-decision.md) 已选路线 A；不保留 Warp Agent UI/SDK/执行层 | 仅清理残留并回归，不重开 A/B 或恢复 Agent UI |
| 云主体 | Drive、cloud object、Agent/blocklist、GraphQL、ServerApi、OfflineApi 主要目录已删除 | R3/R5 清孤儿和引用；完整结构/依赖/产物负证明仍要执行 |
| 八个云 crate、两条协议边 | 既有批次已删除相关目录和 lock 节点 | R5/R6 对当前候选重做 metadata/tree/source/lock/binary 证明，不重复旧目录删除步骤 |
| R0 环境与采集 | [本机网络记录](phase1-acceptance/m5-m6/recovery/network-es-20260912/README.md) 已落实工具链、采集权限、系统 DNS 与完整进程树归因 | 换环境做预检；历史 debug 观测不替代最终产物验证 |
| R1 编译与初始化 | [R1/R2 记录](phase1-acceptance/m5-m6/recovery/r1-r2-20260912/README.md) 包含 library/all-targets/core/TUI check、GUI/TUI 启动和完整 presubmit 的批次通过结果 | 后续改动做回归，不恢复云过渡层或退役 integration harness |
| R2 本地能力 | PTY、Ctrl-C、标签页/分栏/窗口恢复、编辑/搜索/diff、workflow、notebook、env 值、本地日志已恢复并有批次证据 | 保留行为测试与实机回归；不是重新接通这些调用链 |
| 旧数据 | 指定旧版 `066ec71b` 已实际运行并生成 DB；迁移、写入、重启已验证；15 个历史 fixture 原字节保留 | 复用隔离样本，检查最终候选；不再把“尚无真实旧 DB”列为阻塞 |
| LSP、skills、TUI | PATH-only LSP、本地/bundled skills、本地 TUI 已实现；R2 有五种 server 矩阵和真实 TUI PTY 记录 | 保持无下载/无云菜单，最终回归；不新建 slash menu |
| git pin / TUI features | 既有精确 pin 保持；当前 TUI 的 warp 正常/开发依赖均关闭 default，显式启用 local_only/tui，开发依赖含 test-util | R5 核对 manifest/lock 与最终依赖闭包，不重复改配置 |
| 测试删除审计 | R1/R2 已归档用户授权、逐项 ID 审计和 inventory 验证 | R5 对后续改动增量核对；不把历史数量当最新 HEAD 数量或完整覆盖证明 |
| GUI shell 增量 | [2026-09-13 记录](phase1-acceptance/m5-m6/recovery/gui-shell-20260913/README.md) 修复补全、提示符、clear、焦点；12 项 focused tests、严格 Clippy、构建/打包有通过记录 | 完整 presubmit 和最终回归仍未完成；光标闪烁可见相位留人工确认 |
| 更早批次 | 网络守卫、遥测/更新/MCP/AWS/skills 下载链清理、Term4u 身份已落地 | 保留保护与回归，不恢复删除面 |

### 1.2 本次核对的真实剩余入口

| 入口/事实 | 判定 | 归属 |
|---|---|---|
| `app/src/persistence/block_list.rs` 已导入本地 `SerializedBlockListItem` / `SerializedBlock` | 旧 `crate::ai::blocklist` 编译断裂已修复，删除旧待办；不因旧日志重新修同一问题 | R1 已有批次结果，R6 回归 |
| `app/src/settings_view/mod.rs` 只挂载本地 Settings 页面；`privacy_page.rs` 使用 `LocalPrivacyPolicy` | R3 已有部分实现，不能写成全部未开始 | R3 补负面测试和完整入口审计 |
| `app/src/settings_view/tab_menu.rs` 仍引用 `CloudModel` / Teams，但未由当前 Settings 根模块挂载 | 是需要删除或按消费者审计的孤儿源码，不是本次确认的编译错误，也不能据此断言云页面仍可点击 | R3 |
| `script/check_external_product_links` 不存在；`script/presubmit` 尚未调用它 | 检查器、self-test、接入和外链保留清单都未关闭 | R4 |
| `app/src/platform/windows.rs` 仍存在 | Windows 专属清理未满足“仅两个源码例外和历史 migrations”的出口；需审计其他文件/target blocks | R4 |
| `deny.toml` 仍有 `allow-org = { github = ["warpdotdev"] }` | 供应链尚未显式按 git 来源收敛 | R5 |
| `script/presubmit` 调用的是 `test_inventory_tests.py`，不是实际 inventory 全集验证 | 失败处理单测通过不等于当前测试 ID 核对通过；仍需单独运行 `script/test_inventory` | R5/R6 |
| 最新 `gui-shell-20260913/presubmit.log` 在 `clang-format` 缺失处中断 | 工具阻塞；后续 WGSL、workspace nextest、doc tests 未由这次运行证明通过 | R0 预检/R6 |
| `phase1-acceptance/m5-m6/final/manifest.md` 不存在 | 完整 V1 不能关闭 | R6 |

当前核对不是全仓可达性证明。以上已确认入口用于定位后续工作，不能替代 R3–R6 的完整扫描。
`948d475b`、2026-09-05 的缺 Cargo 说明和 R0 记录中的旧编译错误数量均属于历史基线，
不得继续作为当前 HEAD 的错误数或“本地功能尚不可用”的依据。

### 1.3 验收证据的三层边界

1. R0 的指定 debug GUI 620 秒、TUI 65 秒观测通过，只适用于记录的产物 SHA256；不包含
   最终 release/bundle、全部 S1–S13 或第二道系统防火墙验收。
2. R1/R2 随 `d3fd6fdc` 归档，源码身份和结果以
   [current-checkpoint.json](phase1-acceptance/m5-m6/recovery/r1-r2-20260912/current-checkpoint.json)
   及同目录 `candidate-source.json` 为准。其 app 1512 passed / 3 skipped、focused 703 / 2、
   workspace 4752 / 20、completer v2 131 / 4 是该批次数据，不是最新测试统计。
3. `eef716cd` 修改了终端、补全、编辑器及相关测试输入。其 scoped 12 项测试和三组严格
   Clippy 通过，不等于完整 presubmit 通过。补齐工具并对最终冻结候选重跑，不能拼接两次结果。

<a id="constraints"></a>
## 2. 已定边界

1. **路线 A**：删除 Warp Agent UI/SDK/执行层及云协议；禁止用同名空壳、假 URL、空 token、
   `todo!()` 或静默重试修编译。真正本地的值类型移到用途明确的本地模块。
2. 保留 PTY/shell/Ctrl-C、标签页/分栏/窗口恢复、编辑器/文件树/文件搜索/code review、
   本地与项目 workflow、notebook 读写恢复、PATH LSP、本地日志及 bundled skills。
3. 本地 auth 始终无云用户与凭据；不得读取/迁移旧 Warp keyring、`WARP_USER_SECRET`、
   Firebase token 或 refresh token。Term4u keyring 仅供实际需要的本地 secret。
4. 保留历史 migrations、表及旧云数据原始列/JSON；不得通过清表解决加载问题。
   已删除功能的数据不加载到 UI，未知/损坏对象可跳过并记不含正文和密钥的 warning。
5. 保留本地模型的旧 JSON/TOML 字段兼容性。Warp 衍生代码仍为 AGPL，不移入 MIT crate
   冒充原创；不改内部 `warp_*` crate 名，不改产品数据目录和身份。
6. **只支持 macOS**（2026-09-12 用户决定）；Linux 不再是支持平台或验收前提。
   Linux 专属代码、target dependencies、构建/打包脚本和 CI 配置列为后续清理项，
   不随本次状态复核批量删除。清理时保留 macOS 使用的共享代码与混合依赖分支。
   删除 Windows 专属实现与 target dependencies，不清洗全仓内联 cfg；
   保留 `crates/command/src/windows.rs`、`crates/warp_util/src/path/windows.rs` 及历史 migrations。
   混合 dependency 条件中的 macOS 所需依赖不可误删；残留平台分支不代表支持承诺。
7. 本地 HTTP/WebSocket 非 loopback 硬拒绝与无 DNS resolver 不得弱化。普通 shell、git、ssh、
   外部 CLI agent 仍是用户主动运行的独立进程；不新增产品级网络限制来替用户管这些命令。
8. 不新增终端 `model.lock()`；沿用已有锁传递，避免重入死锁。
9. 不靠删保留行为测试、加 `#[ignore]`/`#[allow(dead_code)]`、注释测试、放宽断言或 blanket
   allowlist 变绿。测试放独立 `*_tests.rs`。清单中消失测试逐项说明被删功能、授权或迁移后的新 ID。
   R1/R2 记录中用户已授权整文件删除旧云 integration 及本地接口迁移相关测试，替代旧逐套件
   迁移安排；该历史授权不是未来任意删除保留行为测试的授权。不得恢复退役 harness 来凑覆盖数。
10. 不覆盖用户已有修改，不删除 `.pi/` 来制造干净工作树；不自行 commit/push、创建 PR/tag、
    改全局环境或安装系统依赖。需要这些操作时先取得授权。测试只使用隔离数据副本。

实现前按任务读取仓库 skills：GUI 使用 `gui-ui-guidelines`/`gui-settings-ui`，Rust 测试使用
`rust-unit-tests`；TUI 使用三个 `tui-*` skill；日志使用 `logging-and-error-reporting`。
实机验证按当前保留的 GUI/TUI 能力执行；旧 skills 中依赖已删除 `crates/integration` 的步骤
不再是可执行入口，不得为遵照旧指引恢复云 harness。GUI 实机和真实 TUI PTY 验证仍不可省略。
本文件是执行范围；01–07 章提供设计背景，不重新开启其旧过渡方案。

<a id="batches"></a>
## 3. 施工批次 R0–R6

**当前继续顺序：R0 环境预检 → R3 → R4/R5 → R6。** R4/R5 可在不相互改变验证输入时并行。
R1/R2 保留为已验收的批次边界和必须回归的行为清单；发现新回归才返回对应批次修复。
不再按旧 M5a–M6f 或 R0–R2 顺序重做大删除、接口接通、旧样本生成。

| 优先级 | 下一项交付 | 结束条件 |
|---|---|---|
| P0 | 恢复当前 macOS 验证环境的 `clang-format`、仓库指定 `wgslfmt` 等工具，记录路径/版本 | 工具可执行，先重跑当前完整 presubmit 获取真实新基线；失败如实记录，不删检查 |
| P1 | R3 本地 UI/URI/认证/TUI 入口收尾 | 云入口负面测试和本地页面正向测试、孤儿清单、实机证据齐全 |
| P1 | R4 外链检查器与 Windows 尾项 | checker 正常/self-test 接入 presubmit；保留外链与 Windows 例外精确列举 |
| P1 | R5 显式 git 来源及当前删除/测试清单 | 无组织级放行，来源与许可证有依据；当前 inventory、累计删除集、结构/依赖证明通过 |
| P2 | R6 同一候选完整验收 | §4 全部命令与 C1–C10 证据闭合，生成最终 manifest；然后才进入 M7 |

### R0 · 环境和失败基线刷新（能力已落实，每次开工仍预检）

- 记录当前 HEAD、UTC、工作树初始差异、工具链和可用磁盘；先排查 PATH 与已装 Rust，
  按仓库 pinned toolchain 使用，不擅自升级。记录 `clang-format` / `wgslfmt` 的实际可执行路径。
- 缺工具时优先复用已授权的隔离工具目录；新的系统安装或权限变更仍须授权。历史 `/tmp`
  工具不能假定仍在。最新阻塞不是“没有 Rust”，而是完整 presubmit 缺 `clang-format`。
- 运行下面的 library check，并在环境恢复后运行完整 presubmit；保存完整输出、退出码和根因。
- 新记录放入 `phase1-acceptance/m5-m6/recovery/`，不覆盖历史 baseline/batches。
- 编译未通时测试清单生成失败应记为失败，不能生成空清单当作“全部测试已删除”。
- 本机网络采集与 DNS/子进程归因已有 R0 证据；最终候选执行时仍需验证采集校准有效。
  不再申请 Linux runner，也不以环境问题省略可继续的修复或改变最终验收标准。

```bash
git rev-parse HEAD
git status --short --branch
git diff --check
rustc --version
cargo --version
cargo nextest --version
df -h .
cargo check -p warp --lib --no-default-features --features local_only
```

**产出**：新的 recovery 预检记录、完整日志和错误分组；不引用旧错误计数作为当前诊断。

### R1 · 编译和启动依赖收敛（已有批次验收，后续回归）

R1/R2 的权威记录为 [收敛验收](phase1-acceptance/m5-m6/recovery/r1-r2-20260912/README.md)。
持久化本地类型、初始化、终端生命周期、workspace/pane/root 恢复和 GUI/TUI 检查已有结果。

后续改动继续检查 `app/src/persistence/{sqlite,block_list}.rs`、`crates/persistence/src/model.rs`、
`app/src/lib.rs`、`app/src/terminal/`、`app/src/workspace/`、`app/src/root_view.rs`、
`app/src/search/`、`app/src/settings{,_view}/`、`app/src/app_menus.rs`：

- 不重新引入已删除的 cloud/Agent singleton、协议转换、pane/action 分支或测试初始化。
- 终端历史和窗口恢复使用本地类型；旧 Agent 行保持 opaque，不解码成云协议。
- 本地 notebook、workflow、文件能力及恢复逻辑保持有效，不跳过加载换取启动成功。
- `crates/ai`、`crates/graphql`（若仍存在）和 `app/src/server` 的保留项逐个说明本地用途；
  无本地消费者的模块删除。允许纯本地 ID 和 no-op telemetry 类型，不保留云 provider。
- 追踪 tests、bin 和 feature unification；不只验证 `--lib`。

**回归出口**：§4.1 check 全过、GUI/TUI 可启动；不得把已删除 integration crate 加回依赖图。

### R2 · 本地数据和核心行为闭环（已有批次验收，最终候选仍回归）

| 面 | 必须保持并在最终候选复验的行为 |
|---|---|
| PTY/终端 | shell 启动、命令输出、Ctrl-C、退出；GUI 标签页/分栏/窗口重启恢复；外部 CLI 作为普通 PTY 子进程；最新补全、提示符、clear 和焦点修复 |
| 编辑器/文件 | 文件树打开、本地搜索、打开/编辑/保存文件、code review 与本地 diff；未保存取消关闭、Undo 和 pane 恢复 |
| workflow | 本地与项目 `.warp/workflows` 加载并执行；旧 JSON/TOML 名称、命令、参数和编辑字段一致；默认值/覆盖值及缺必填参数行为保留 |
| notebook | 旧 SQLite/JSON 打开、编辑、换行、退出、重启后内容一致；本地新版本不被旧行反向覆盖 |
| env vars | 本地值解析与 shell 展开保持；不恢复 Drive collection/权限/分享 |
| 旧 DB | 迁移、读取历史/设置/窗口、写入、再次启动；被删云对象隐藏，损坏对象不阻止其他数据恢复；未知/缺省字段兼容 |
| 日志 | 正常日志、debug panic 日志、UI 本地 ZIP 均有效且无上传；warning 不含正文/secret |
| LSP/skills | 五种 server 的 PATH 命中、缺失和不可运行分支；缺失只提示手工安装，无版本查询/下载；bundled/local skills 可读且不自动更新 |

指定旧版已生成真实样本，见 R1/R2 的 `oracle-066ec71b/`；fixture 现位于
`crates/persistence/fixtures/legacy`。复用隔离副本，保存旧/新字段、写入与重启对照。
旧版自定义标题未持久化等已记录限制仍需保留，不能扩大其覆盖声明。新的样本替代或范围豁免
仍需明确批准。不得恢复旧 cloud 解析器到产品依赖树。

最新 GUI 增量的光标闪烁可见相位仍需人工确认；focused tests 通过和截图未捕获到该相位是
不同结论。后续改动相关输入后也须重新验证补全、prompt、clear 及选择/焦点行为。

**回归出口**：上述每行对应可运行本地测试和实机记录；§4.1 完整集合通过。

### R3 · UI、认证和 TUI 残留收尾（部分实现，尚未验收）

从当前 `SettingsSection`、`settings_view/mod.rs`、菜单/action/URI 注册和 TUI 入口反向追踪，
先列消费者和可达性，再删除孤儿。`tab_menu.rs` 是本次确认的未挂载云引用样例，不是完整清单。

- Settings、Command Palette、菜单、workspace actions、URI/deeplink 不再注册登录/退出账号、
  Drive、团队、计费、推荐、Platform/API key、Environments、Warp Agent/Agent Profiles、
  shared session、cloud run、handoff、升级等入口；清理孤儿页面、flag 和 keybinding。
- 已有 `PrivacyPageView` / `LocalPrivacyPolicy` 保持固定本地值；保留本地
  Appearance/Features/Keybindings 等实际页面，不重新实现已完成的 Privacy。
- Settings 搜索 `drive`、`team`、`billing`、`api key`、`warp agent` 不出现云页面；
  本地页面仍可搜索。无灰色云按钮、幽灵搜索结果或“登录后使用”壳。
- 本地 auth 保持无凭据；若类型仅为旧数据解析用途，收敛成中立值类型，不增加云 auth API。
- TUI 维持本地 zero state/transcript/tab；`/` 是普通 shell 输入，**不新增 slash menu**。
  固定宽度 render-to-lines、快捷键注销、命令/Ctrl-C/tab/退出均回归。
- 维护者/agent 指引不得继续要求已删除的 cloud server 或 integration harness。按当前入口
  修正执行指引，不借文档收尾重建这些能力；完整对外品牌改写仍留 M7。

**出口**：Settings/URI/菜单/TUI 测试、源码与注册清单、GUI 截图/TUI 屏幕文本齐全。
“未挂载所以不编译”不是源码删除通过，也不自动证明所有云 UI 不可达。

### R4 · 外链和 Windows 尾项（明确未完成）

- 审计 `open_url` 与 URL 常量，删除内建 Warp Privacy/Data Management/Docs/Issues/Slack/
  Drive/Oz/billing/upgrade/sign-in/out 外链。保留用户主动点击终端或文件中的普通链接。
- 新增 `script/check_external_product_links`，有允许/拒绝样例及 `--self-test`，接到
  `script/presubmit` 的 clippy 之前。扫描错误也必须失败，不得用 `|| true` 吞掉。
- 保留外链逐项记录路径、触发方式、理由，生成 `final/external-link-allowlist.txt`。
- 按 §2 的例外收敛 app/warpui/warpui_extras/prevent_sleep/warp_terminal/warp_util 的 Windows
  专属文件、模块挂载、纯 Windows target/build dependencies；混合条件保留 macOS 所需部分。
  从当前仍存在的 `app/src/platform/windows.rs` 等文件开始核对，不按旧目录数量推算完成度。
- 清理无调用的 `embed-resource`、`dunce` 等依赖（已删除的无需重做）。生成最终 Windows
  专属文件及 target block 审计表，不能仅凭“不构建 Windows”验收。

**出口**：网络/外链两种 boundary checker 的正常检查与 self-test 通过；Windows 剩余项
严格等于已定例外。Linux 清理是已登记的后续范围，不混入本轮平台验收。

### R5 · manifest、供应链与删除清单（部分已落地，需增量收尾）

- 删除失效 workspace/member/dependency/feature/patch；复核 ai/persistence/TUI 的正常和
  开发依赖，并确认退役 integration crate/入口未被拉回；默认 feature/test-util 不得恢复云实现。
- 维持 TUI 对 warp 的 `default-features = false` 和显式 local_only/tui/test-util 配置。
- 维持 `email_address` rev `b3e5205b4efdc83832f230d8eb6894fd00a9ec32`，manifest/lockfile 一致。
- 删 `deny.toml` 的 `allow-org`，显式列出全部保留 git 来源及许可证依据；禁止为已删除协议
  加白名单。同步 `about.toml`/`deny.toml` 与 NOTICES 输入，不做无关依赖升级，不手改 checksum。
- 在 R1/R2 已归档授权和 `test-removal-audit.json` 基础上增量核对，不从零重做旧套件迁移。
  `deleted-test-ids.txt` 必须仍是相对 `baseline/phase1-before.txt` 实际消失 ID 的精确全集；
  后续新增删除/恢复分别更新，保留行为覆盖缺口随实现补齐。历史审计通过不等于当前 inventory 通过。
- 归档逐批删除集，`script/deletion_set.txt` 最终覆盖累计 M5/M6 实际删除范围；复核与当前源码
  的差异，不把旧单批清单或一次授权当作全仓闭合证明。

R1/R2 的 9777 个基线 ID、4772 个当前 ID、5089 个累计消失和 84 个新增是该批次统计，
并不表示所有消失测试都在该提交删除或逐项迁移。`eef716cd` 已新增/修改测试，最终应重新
执行 `./script/test_inventory`；`presubmit` 内的 inventory 失败处理单测不能替代此步骤。

**出口**：§4.2–4.3 通过，无未批准测试消失；保留 git 来源明确，结构/源码/依赖/产物负证明齐全。

### R6 · 最终工程、运行时和 macOS 验收（未完成）

R3–R5 实现收尾后冻结候选源码，执行 §4 全部检查，对照 C1–C10 逐项归档。失败回到对应批次
修复，再冻结并重跑。当前没有最终 manifest，不允许宣布“V1 代码完成，仅差验证”。

R1/R2 的完整 presubmit、R0 的 debug 网络观测、最新 GUI 的 focused 回归分别保留，但不能
拼成最终 PASS。最新 `clang-format` 阻塞恢复后，仍需完整 WGSL、workspace/completer/doc tests、
构建矩阵、release/bundle 负证明和相同产物的实机/网络验证。

<a id="verification"></a>
## 4. 必跑验证

所有命令从仓库根目录运行；保存命令、UTC、退出码、原始输出。管道启用 `set -euo pipefail`，
Cargo/扫描失败与“无匹配”分开处理。以下是出口命令，不意味着本次文档更新已执行通过。

### 4.1 快速编译和 focused tests

```bash
cargo check -p warp --lib --no-default-features --features local_only
cargo check -p warp --all-targets --tests --no-default-features --features local_only
cargo check -p warp_tui --all-targets --tests
cargo check -p ai -p persistence -p warp_terminal -p lsp --all-targets --tests
cargo nextest run -p warp --no-default-features --features local_only,test-util
cargo nextest run -p warp_tui -p ai -p persistence -p warp_terminal -p lsp
./script/test_inventory
./script/check_network_boundaries
```

施工中可按实际测试 ID 过滤，但须确认过滤器命中测试且通过，最终执行以上完整集合。

### 4.2 工程门禁与构建

```bash
./script/format
./script/format --check
git diff --check
./script/check_no_inline_test_modules
./script/check_network_boundaries --self-test
./script/check_network_boundaries
./script/check_external_product_links --self-test
./script/check_external_product_links
./script/check_license_boundaries
./script/check_license_config_sync
./script/test_inventory
cargo clippy -p warp --all-targets --tests --no-default-features --features local_only -- -D warnings
cargo clippy -p warp_tui --all-targets --tests -- -D warnings
./script/presubmit
cargo test --workspace
cargo test --workspace --doc
cargo deny check sources licenses bans advisories
cargo clippy -p warpui_core --features tui --all-targets -- -D warnings
cargo build -p warpui_core --bin tui_integration --features tui
cargo clippy -p editor -p warp_tui --features test-util --benches -- -D warnings
cargo build --workspace
cargo build --locked -p warp --bin term4u --features offline_hard
cargo build --locked -p warp --bin term4u --no-default-features --features local_only
cargo build --locked -p warp_tui --bin term4u-tui \
  --no-default-features --features offline_hard,standalone
cargo build --release --locked -p warp --bin term4u --no-default-features --features local_only
cargo build --release --locked -p warp_tui --bin term4u-tui \
  --no-default-features --features offline_hard,standalone
```

`presubmit` 须保留 workspace/GUI/completer 三种 clippy、workspace nextest、completer v2、doc tests、
clang-format、WGSL 检查。F7–F9 不能因 presubmit 未涵盖而跳过；不额外把已知不支持的
`--all-features` 设为通过条件。缺工具先恢复环境并重跑，不删除或绕过检查。

### 4.3 结构、源码、依赖及产物负证明

下面片段在同一 Bash 会话按序执行。原始输出归档后再判定；扫描出错不能被解释成无匹配。

```bash
set -euo pipefail
E=docs/redesign/phase1-acceptance/m5-m6/final
mkdir -p "$E"
no_match() {
  local rc=0
  "$@" || rc=$?
  if [ "$rc" -ne 1 ]; then
    echo "FAIL: expected no matches (exit 1), got exit $rc" >&2
    return 1
  fi
}
BLACKLIST='sentry|opentelemetry|rudder|warp_server_client|warp_server_auth|warp_multi_agent|cloud_object|firebase|session-sharing|computer_use|voice_input|remote_server|\bmcp\b|rmcp|aws-sdk|aws-smithy'
cargo metadata --locked --no-deps --format-version 1 > "$E/metadata.json"
jq -r '.packages[].name' "$E/metadata.json" > "$E/workspace-packages.txt"
no_match rg -i "$BLACKLIST" "$E/workspace-packages.txt"
cargo tree --locked -p warp --no-default-features --features local_only -e normal \
  > "$E/cargo-tree.txt"
cargo tree --locked -p warp_tui --no-default-features --features offline_hard -e normal \
  > "$E/cargo-tree-tui.txt"
no_match rg -i "$BLACKLIST" "$E/cargo-tree.txt" "$E/cargo-tree-tui.txt"
no_match rg -n 'name = "(warp_server_client|warp_server_auth|firebase|warp_multi_agent_client|cloud_object_client|cloud_object_models|cloud_object_persistence|cloud_objects|session-sharing-protocol|warp_multi_agent_api)"' Cargo.lock
no_match rg -n 'session-sharing-protocol|warp-proto-apis|branch = "main"' \
  Cargo.toml app/Cargo.toml crates --glob Cargo.toml
no_match rg -n 'ServerApiProvider|OfflineApi|trait (AIClient|ObjectClient|AuthClient)|warp_server_client|warp_server_auth|warp_multi_agent_client|warp_multi_agent_api|session_sharing_types' \
  app/src crates --glob '*.rs' --glob Cargo.toml
no_match rg -n 'CloudModel|CloudViewModel|SyncQueue|UpdateManager|ObjectClient|http_client' \
  app/src/local_objects app/src/workflows app/src/notebooks app/src/env_vars --glob '*.rs'
no_match rg -n 'fetch_latest_server_metadata|install_from_github|fetch_npm_package_version|GITHUB_API_URL' \
  crates/lsp app/src --glob '*.rs'
for path in \
  app/src/drive app/src/cloud_object app/src/server/cloud_objects app/src/server/sync_queue.rs \
  app/src/settings/cloud_preferences_syncer.rs app/src/ai/agent app/src/ai/agent_sdk \
  app/src/ai/agent_management app/src/ai/blocklist app/src/ai_assistant \
  app/src/server/graphql app/src/server/server_api.rs app/src/server/server_api \
  app/src/server/offline_api.rs crates/warp_tui/src/cloud_run.rs crates/warp_tui/src/handoff \
  crates/warp_server_client crates/warp_server_auth crates/firebase crates/warp_multi_agent_client \
  crates/cloud_object_client crates/cloud_object_models crates/cloud_object_persistence crates/cloud_objects; do
  test ! -e "$path" || { echo "FAIL: removed path remains: $path" >&2; exit 1; }
done
```

另存 Auth/UI 残留、全部 Windows 文件/target blocks、外链、feature、下载脚本与历史 migration
的审计输出；逐项对应 R2–R5。迁移保护需与 `94912b78` 及开工 HEAD 对比，既检查删除也审查
修改，禁止改写历史迁移或新增破坏旧数据的迁移。旧表名/opaque JSON 不是恢复云实现的理由。

`aws-lc-rs`/`aws-lc-sys` 是 TLS 密码学后端，保留；不得用宽泛 AWS 模式误杀。
`sts.googleapis.com` 不是 AWS STS，按实际调用用途审计。

```bash
PATTERN='app\.warp\.dev|rtc\.app\.warp\.dev|sessions\.app\.warp\.dev|oz\.warp\.dev|releases\.warp\.dev|rudder|sentry|otlp|opentelemetry|firebase|AIzaSy|warp drive|session sharing|aws bedrock|mcp'
strings target/release/term4u > "$E/gui-strings.txt"
strings target/release/term4u-tui > "$E/tui-strings.txt"
no_match rg -i "$PATTERN" "$E/gui-strings.txt" "$E/tui-strings.txt"
# macOS：构建并扫描实际发布 bundle 及其全部 resources。
cargo bundle --release --bin term4u --no-default-features --features local_only
no_match rg -a -n -i "$PATTERN" target/release/bundle/osx/Term4u.app
```

若有真正不可避免的假阳性，先记录精确字符串、来源、保留理由、负责人批准，再按精确项过滤。
保存原始命中和过滤结果；不得自动忽略 `warp`、`mcp`、全部 resources 或整个依赖组织。
对发布二进制及 bundle 记录 SHA256，确保扫描和实机运行的是同一产物。

### 4.4 实机、旧数据和网络

- **GUI**：S1–S13 全执行：启动、标签页、分栏、命令、文件树、文本预览、设置、失焦/聚焦、
  睡眠/唤醒、关窗、退出、测试构建崩溃、空闲十分钟。另测 R2 的编辑保存、文件搜索、code review、
  workflow 执行、notebook 编辑后重启与窗口恢复，记录观察而非只截图启动页；加入最新 shell 回归。
- **TUI**：在真实交互终端运行 `./script/run-tui`，验证启动、输出、Ctrl-C、tab、新 tab、退出，
  保存屏幕文本；有 tmux 用 tmux，否则 PTY。GUI 测试不能代替 TUI 实机。
- **数据**：R2 旧 DB/fixture 的迁移、历史条数及字段对照、写入与重启；只操作隔离副本。
  debug 可用 `WARP_DATA_PROFILE`，release 使用隔离 HOME；数据/日志实际路径从当前实现确认，
  不照抄旧 `.warp-oss` 路径，也不杜撰 `TERM4U_DATA_DIR`。
- **日志**：实际验证正常写入、debug panic 入日志、UI 导出 zip；不要用 SIGSEGV 成功退出
  代替 Rust panic 日志验证。原始日志归档前检查 secret，不展示用户正文。
- **LSP**：伪 PATH server 记录启动参数；PATH 缺失给手工提示；伪 curl/wget/npx/npm 下载调用日志为空。
- **网络**：隔离 HOME/干净 shell/恶意代理下，真实候选 GUI/TUI 各至少运行 60 秒；采集主进程
  和产品启动子进程的 TCP/UDP/WebSocket、DNS 53/853 及后台重试证据；S13 再空闲十分钟。
  零非 loopback 外连、零 DNS、零重试、零待发遥测/崩溃文件，不以“连接被守卫拒绝”代替无请求。
  DNS 必须能归因或在隔离环境捕获，不能只 grep 进程名；采集失败不算零。
- 系统防火墙作为第二道防线需记录拒绝日志并验证为空；修改规则需授权。上述静默运行场景不
  主动运行联网命令；另行验证用户 PTY 子进程可用，不把用户主动网络混进产品零外连指标。

### 4.5 macOS 验收矩阵

仅本机 macOS arm64 为必验平台；网络捕获也在本机进行，不再申请 Linux runner。
捕获权限、进程/DNS 归因和第二道防线需针对本轮实际验证；历史 R0 校准成功不保证新采集有效。

| 平台 | 最小必验 |
|---|---|
| macOS arm64 | §4 工程门禁、GUI/TUI 实机、旧 DB、网络、LSP、`.app`/resources、签名状态、scheme、keyring/数据目录身份 |

本机矩阵全部通过才算 PASS；产品失败为 FAIL，权限或工具缺失为 INCOMPLETE。
Linux 相关代码与配置后续清理，不把不再要求 Linux 验证写成 Linux PASS。

<a id="acceptance"></a>
## 5. 验收条件 C1–C10

下表增加本次复核状态，但**不降低原有最终条件**。批次证据与最终验收分开，不按删除行数
计算完成比例；当前没有同一最终候选的 C1–C10 完整 PASS。

| ID | 最终完成条件与权威证据 | 2026-09-13 复核状态 |
|---|---|---|
| C1 | GUI/TUI 及关联 crate 编译、default/local_only 与 test-util 组合通过；§4.1/4.2 原始 check/build 日志、退出码、候选源码身份 | R1/R2 有批次 check/build，最新有局部 GUI 构建；最终完整组合待重跑 |
| C2 | PTY、Ctrl-C、tab/split/窗口恢复、文件树/搜索/编辑保存/code review 保留；本地自动化测试及 GUI/TUI 实机记录 | R2 批次通过；最新 shell 修复有 scoped 证据，光标可见相位和最终回归未关闭 |
| C3 | 本地/project workflow、notebook、env-var 和旧 DB 可读写恢复；新旧 fixture、真实样本或批准替代、迁移审计、重启对照 | R2 已有真实旧版样本和迁移/写入/重启证据；最终候选复验 |
| C4 | 云 UI/认证/Agent/Drive/共享入口及死引用清零，本地设置和 TUI 正常；Settings/URI/menu/render 测试、截图、源码/结构扫描 | R3 部分实现；孤儿源码与完整负面/正向入口证明未关闭 |
| C5 | LSP 只用 PATH、本地 skills/日志完整、外链和 Windows 尾项完成；PATH/零下载、日志三场景、资源、外链 self-test、Windows 例外审计 | LSP/skills/日志有 R2 证据；外链 checker 缺失、Windows 尾项未完成 |
| C6 | 云 crate/协议/ServerApi 不在 workspace、正常依赖树、源码与 lockfile；metadata/tree/源码/lock、精确 pin、显式 git 来源、cargo deny | 删除/feature 有实现基础；组织级放行仍在，完整供应链及负证明未关闭 |
| C7 | 无未批准测试消失，保留行为测试未被削弱，全部工程门禁通过；inventory、授权/删除/迁移 ID、累计删除集、format/clippy/presubmit/workspace/doc/F7–F9 日志 | R1/R2 已有删除审计及完整 presubmit；最新完整 presubmit 缺工具中断，当前 inventory/完整门禁待重跑 |
| C8 | 最终 GUI/TUI 二进制和 bundle 无未批准黑名单命中；静默运行零外连/DNS/重试/待发文件；hash、strings/resources、60 秒捕获、S1–S13/十分钟空闲、防火墙记录 | R0 只覆盖指定 debug 产物；最终 release/bundle、相同产物运行和第二道防线证据缺口仍在 |
| C9 | macOS arm64 最小矩阵通过；同一候选的本机命令、退出码、实机与产物记录 | 有多个批次的局部 macOS 结果，尚未形成同一最终候选矩阵 |
| C10 | R0–R6 收敛，同一候选证据完整；final/manifest.md 的 C1–C10 索引、源码与证据提交对应、最终 diff/status 审计 | 最终 manifest 尚不存在；V1 未完成 |

<a id="delivery"></a>
## 6. 证据与交付

继续使用既有验收目录，不重写历史 PASS，也不建立另一套相互竞争的验收目录：

```text
docs/redesign/phase1-acceptance/m5-m6/
├── baseline/                 # 保留原始基线
├── batches/                  # 保留 M5b/M5g 历史记录
├── recovery/                 # 批次、预检、失败、删除/迁移清单与总结
└── final/
    ├── manifest.md           # C1–C10；每项命令、UTC、退出码、证据路径、结论
    ├── repository-state.txt  # SOURCE_HEAD、初始/最终工作树、工具链、授权记录
    ├── cargo-tree.txt
    ├── cargo-tree-tui.txt
    ├── source-scans.txt
    ├── tests-diff.txt
    ├── presubmit.log
    ├── external-link-allowlist.txt
    ├── macos.md
    └── platform-scope.md     # macOS-only；Linux 后续清理范围
```

最终验证前，经授权把源码、manifest、lockfile、测试、脚本、资源冻结到候选提交 `SOURCE_HEAD`。
全部最终检查针对同一个候选，记录构建参数与产物 hash；失败后改动这些输入即重新冻结并重跑。
历史 baseline/batches/recovery 的 HEAD 和失败无需篡改成新 HEAD 或新结论。

验收证据可随后以**仅证据/状态文档**的提交归档；记录该提交与 `SOURCE_HEAD` 的关系，并证明
差异不含构建/运行/测试输入。不要求 manifest 写入其自身提交 hash，避免自引用；若归档提交改变
任何验证输入，旧证据立即失效。未经授权不得为凑交付条件自行提交或推送。

最终审计确认任务变更已完整归档、无遗漏源码修改；初始用户文件和 `.pi/` 的保留状态另列，
不得为使 `git status` 为空而删除。没有提交权限、必需平台或可归因捕获时如实记录未完成，
继续可执行工作，不削减 C1–C10。V0 严格验收的关闭在 `phase1-acceptance/final/manifest.md`
建立指向本批证据的索引，避免重复复制日志；同步 08 章、本索引及 `baseline/risk-log.md`。

本次文档复核不生成最终 PASS manifest，也不重新签发历史测试结果。
最终条件全部达成后才可宣称 **M5/M6 与 V1 完成，V0 严格验收关闭**。M7 品牌/许可证发布材料、
首个 tag 与完整 MIT 阶段仍是后续任务，不能顺带标为完成。

<a id="goal"></a>
## 7. Goal 指令

在 Term4u 仓库、已加载 Goal 扩展的会话中粘贴；创建合同时确认 C1–C10 与验证步骤没有遗漏。
不使用 `--raw` 绕过结构化验收，也不把“阅读/更新施工文档”当作下一轮开发目标：

```text
/goal 按 docs/redesign/11-本地化收敛施工单.md 继续 Term4u M5/M6 本地化收敛。先做 R0 环境预检并恢复完整 presubmit 所需工具，以已归档 R1/R2 为保留行为基线，完成 R3、R4、R5，再冻结同一候选执行 R6，满足 C1–C10 并归档证据。不要重做已完成的大删除、本地接口接通或旧 DB 样本生成；后续发现回归再返回 R1/R2 修复。不得恢复云空壳/退役 integration harness、删保留行为测试绕过验收、拼接不同源码的 PASS 或把未验证当完成；不扩展到 M7/MIT 重实现。提交、推送、系统安装及权限变更仍按授权边界执行。
```
