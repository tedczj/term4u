# 03 · 阶段 1 —— 云模块删除与离线化

> 本章是 V0/V1 的施工图。事实来源全部是 [02 章](02-现状审计.md)；调研原文见
> [附录 A](附录A-上游调研原文.md)。
>
> **阅读顺序提示**：先看 §1 的总表定"删什么"，再看 §2 定"能省下哪些 git 依赖"，
> 然后 §3–§7 是五个必须**按顺序**做的结构性改动，§8–§11 是四个"不删而是替换"的
> 特例，§12 是唯一的待定项。

<a id="toc"></a>
## 目录

- [一、模块处置总表](#s1)
- [二、删除 → git 依赖消失映射](#s2)
- [三、入口设计](#s3)
- [四、ConnectivityMode 与 Channel::Offline](#s4)
- [五、initialize_app 三分](#s5)
- [六、feature 重构](#s6)
- [七、网络硬拒绝：在 socket 对端地址层](#s7)
- [八、ServerApi 空实现（null object）而非删除](#s8)
- [九、遥测：保留 trait + 宏 shim，只删 collector 与传输](#s9)
- [十、LocalPrivacyPolicy 固化](#s10)
- [十一、持久化：迁移不删](#s11)
- [十二、待定：`app/src/ai/blocklist` 的三条路线（V1 决策点）](#s12)

---

<a id="s1"></a>
## 一、模块处置总表

处置动作的定义：

| 动作 | 含义 |
|---|---|
| **删** | 物理删除文件/目录，并从 `mod` 树摘除 |
| **空实现** | 保留类型与签名，方法体改为返回离线错误或空值（null object） |
| **shim** | 保留 trait/宏定义，实现体变为 no-op |
| **降级** | 保留 UI，接不到后端时显示明确的"本地模式不可用"，不重试、不报错 |
| **保留** | 不动 |
| **收紧** | 保留但加约束（如网络守卫） |

时机列：**V0** = 可日常使用；**V1** = 依赖树干净；**V2** = 品牌与发布；**阶段2** = 完整 MIT。

<a id="s1-1"></a>
### 1.1 上报与追踪

| 模块 | 源文件/行 | 处置 | 时机 | 依据 |
|---|---|---|---|---|
| `app/src/server/telemetry/` | 8 文件 / 8,346 行 | **删** | V0 | 调研 §二.A |
| `app/src/server/telemetry_ext.rs` | 1 / 127 | **删** | V0 | 同上 |
| `crates/warp_core/src/telemetry.rs` | 262 行 | **shim**（保 trait + 6 个宏，实现体清空） | V0 | [02 §7.10](02-现状审计.md#s7-10)：删它 = 改 212 文件 / 888 处 |
| `crates/warpui_core/src/telemetry/`、`app_focus_telemetry.rs` | 5 文件 | **shim** | V0 | **位于 MIT 岛内**，改动需加双版权行（[04 §4](04-残余依赖与许可证架构.md#s4)） |
| 11 个 `*/telemetry.rs` 事件生产者（`app/src/tui/telemetry.rs` 等） | — | **保留**（宏已空） | V1 再删 | 调研 §二.A 末段 |
| `app/src/crash_reporting/` | 4 / 1,194 | **删** | V0 | 调研 §三.A |
| `app/src/tracing/native.rs` | 1 / 542 | **删** | V0 | 调研 §三.B |
| `app/src/tracing/cloud_agent_auth.rs` | 1 / 547 | **删** | V0 | 同上 |
| `app/src/tracing.rs` `init()` | — | **改**：固定装 `NoSubscriber` 或仅本地文件 subscriber | V0 | 同上 |
| `script/sentry_upload_dif.sh`、`sentry_create_release.sh`、`macos/update_sentry_cocoa` | 3 脚本 | **删** | V0 | 发布用途，不影响本地 |

<a id="s1-2"></a>
### 1.2 认证与云控制面

| 模块 | 源文件/行 | 处置 | 时机 | 依据 |
|---|---|---|---|---|
| `app/src/auth/` | 14 / 6,561 | **删云认证**（Firebase / SSO / device auth / token 刷新），**留** `LocalAuthStateProvider` 空实现 | V0 | 调研 §四.2；60+ 测试依赖 `AuthStateProvider` |
| `crates/warp_server_auth/` | 7 / 1,302 | **删** | V1 | 依赖树清理 |
| `crates/warp_server_client/` | 10 / 2,529 | **删** | V1 | 同上 |
| `crates/firebase/` | 1 / 145 | **删** | V1 | 同上 |
| `app/src/server/server_api.rs` + `server_api/`（19 文件） | 1,493 + 子模块 | **空实现**（见 [§8](#s8)） | V0 空实现 / V1 评估删除 | **决策 D4**：保 UI 外壳需要它活着 |
| `app/src/server/experiments/`、`app/src/experiments/` | 3+5 文件 | **删**（远程实验获取），保留编译期固定 flag 集 | V0 | 调研 §四.3 |
| `app/src/server/iap_identity_minter.rs` | 1 / 43 | **删** | V0 | 叶子 |
| `app/src/server/voice_transcriber.rs` | 1 / 48 | **删** | V0 | 叶子 |
| `app/src/server/sync_queue.rs` | 1 / 1,988 | **删** | V0 | 调研 §七 |
| `app/src/server/cloud_objects/` | 5 / 5,805 | **删** | V0 | 同上 |
| `app/src/server/graphql/` | 3 / 213 | **删** | V1 | 随 ServerApi |

<a id="s1-3"></a>
### 1.3 Agent 与 AI

| 模块 | 源文件/行 | 处置 | 时机 | 依据 |
|---|---|---|---|---|
| `app/src/ai/agent/` | 20 / 17,799 | **删** | V0 | 调研 §五 |
| `app/src/ai/agent_sdk/` | 58 / 32,628 | **删** | V0 | 同上 |
| `app/src/ai/agent_management/` | 12 / 6,736 | **删** | V0 | 同上 |
| `app/src/ai/agent_conversations_model.rs` | 1 / 2,119 | **删** | V0 | 同上 |
| `app/src/ai/ambient_agents/` | 7 / 1,991 | **删** | V0 | 同上 |
| `app/src/ai/cloud_environments/`、`cloud_agent_config/`、`cloud_agent_settings.rs`、`connected_self_hosted_workers.rs` | — | **删** | V0 | 同上 |
| `app/src/ai/artifacts/`、`artifact_download.rs` | 3 / 1,039 | **删** | V0 | 调研 §六 |
| `app/src/ai/predict/`、`get_relevant_files/`、`remote_agent_context.rs`、`restored_conversations.rs`、`request_usage_model.rs`、`conversation_rename.rs`、`conversation_utils.rs` | — | **删** | V0 | 调研 §五 |
| `app/src/ai/voice/` | 5 / 160 | **删** | V0 | 调研 §十二 |
| `app/src/ai_assistant/` | 7 / 3,312 | **删** | V0 | 调研 §五 |
| **`app/src/ai/blocklist/`** | **149 / 98,892** | **【待定】** V0 按"编译保留 + 降级"处理 | V1 决策 | 见 [§12](#s12) |
| `crates/computer_use/` | 38 / 10,468 | **删** | V0 | 调研 §六 |
| `crates/warp_multi_agent_client/` | 1 / 171 | **删** | V1 | 依赖树清理 |
| `app/src/ai/mcp/gallery.rs`、`templatable.rs`、`templatable_installation.rs`、`templatable_manager/`、`manager/` 中的云路径 | — | **删**（云 MCP） | V0 | 调研 §九 |
| `FileMCPWatcher`（`app/src/ai/mcp/file_mcp_watcher.rs`）/ `FileBasedMCPManager`（`app/src/ai/mcp/file_based_manager.rs`）/ 本地 stdio MCP | — | **保留** | — | 调研 §九"可以保留" |

<a id="s1-4"></a>
### 1.4 Drive / 云对象 / 共享 / 远端

| 模块 | 源文件/行 | 处置 | 时机 | 依据 |
|---|---|---|---|---|
| `app/src/drive/` | 39 / 20,827 | **删** | V0 | 调研 §七 |
| `app/src/cloud_object/` | 10 / 4,439 | **删** | V0 | 同上 |
| `crates/cloud_objects/`、`cloud_object_client/`、`cloud_object_models/`、`cloud_object_persistence/` | 37 / 6,971 | **删** | V1 | 依赖树清理 |
| `app/src/settings/cloud_preferences_syncer.rs` | 1 / 1,013 | **删** | V0 | 调研 §七 |
| `app/src/terminal/shared_session/` | 29 / 11,746 | **删** | V0 | 调研 §八 |
| `app/src/remote_server/` | 17 / 8,298 | **删** | V0 | 调研 §十一 |
| `crates/remote_server/` | 15 / 7,565 | **删** | V0 | 同上（含 `install_remote_server.sh`，会在 SSH 远端 curl 下载 tarball） |
| `app/src/autoupdate/` | 6 / 3,100 | **删** | V0 | 调研 §十 |
| `app/src/changelog_model.rs` | 1 / 247 | **删** | V0 | 同上 |
| `crates/voice_input/` | 1 / 526 | **删** | V0 | 调研 §十二 |
| `app/src/integration_testing/warp_drive/`、`cloud_object/` | 4 / 142 | **删** | V0 | 随模块 |

**删除集合计：408 个源文件 / 176,188 行**（[02 §7.7](02-现状审计.md#s7-7)）。

<a id="s1-5"></a>
### 1.5 保留但收紧

| 模块 | 处置 | 时机 | 说明 |
|---|---|---|---|
| `crates/http_client/` | **收紧**：`execute_inner` + `eventsource` 加 socket 对端地址守卫 | V0（M1） | [§7](#s7) |
| `crates/websocket/` | **收紧**：`native.rs::connect` 加同样守卫。**不删**——`warp_errors` 的可选 feature 依赖它 | V0（M1） | 同上 |
| `crates/lsp/` | **保留运行时，关自动下载**：`install.rs` + 5 个 `servers/*.rs`（rust/go/clangd/pyright/typescript）只查 PATH，缺失时提示手工安装 | V0 | 调研 §十三.2 |
| `asset_cache::url_source`（在线字体） | **改**：远程 URL 直接返回拒绝错误 | V0 | 调研 §十三.1 |
| `app/src/settings/privacy.rs` | **改**：见 [§10](#s10) | V0 | |
| `crates/persistence/`（141 迁移 / 50 表） | **保留，一个都不删** | — | [§11](#s11) |
| `app/src/terminal/`（除 `shared_session/`） | **保留** | — | 核心 |
| `crates/warp_terminal/`、`crates/warpui{,_core,_extras}`、`app/src/editor/`、`app/src/code/`、`app/src/pane_group/`、`app/src/tab.rs`、`app/src/tab_configs/`、`app/src/themes/`、`app/src/keyboard.rs`、`app/src/default_terminal/`、`app/src/search/`、`app/src/workflows/local_workflows.rs` | **保留** | — | 核心 |
| `bundled_skills`（**在 default 内**，`app/Cargo.toml:620`） | **改**：默认只加载本地已审查 skill，不拉 Warp 官方在线 skill | V0 | [02 §6.3](02-现状审计.md#s6-3) |
| `skills-lock.json` + `script/resolve_common_skills` | **删构建期网络**：见 [05 §5](05-阶段1-仓库与品牌.md#s5) | V0 | [02 §5.5](02-现状审计.md#s5-5) |

---

<a id="s2"></a>
## 二、删除 → git 依赖消失映射

| git 依赖 | 依赖它的 crate | 删除后是否消失 | 说明 |
|---|---|---|---|
| `session-sharing-protocol` | `cloud_object_models:27`、`cloud_object_persistence:24`、`cloud_objects:23`、`warp_server_client:50`、**`warp_terminal:49`**、`app:188` | **不会自动消失** | 前 4 个 crate 随 V1 删掉；`app` 的边随 `shared_session/` 删掉；**但 `warp_terminal` 要留下** → 必须**手工剥离**这条边 |
| `warp_multi_agent_api`（`warp-proto-apis`） | `ai:47`、`integration:45`、**`persistence:24`**、`warp_multi_agent_client:23`、`app:262` | **不会自动消失** | `warp_multi_agent_client` V1 删；`ai` / `app` 的边随 agent 删；`integration` 的边随集成测试体删；**但 `persistence` 要留下** → 必须**手工剥离** |
| `warp-command-signatures`（+ 传递的 `warp-completion-metadata`） | `integration:44`、`warp_completer:42,:47`、`app:344,:369` | **保留** | 补全引擎，与云无关 |
| `command-corrections` | `warp_core:20`、`warp_terminal:22`、`app:85` | **保留** | 命令纠错，本地能力 |
| `mermaid_to_svg` | `editor:36`、`app:151` | **保留** | 本地渲染 |
| `warp-workflows`（+ 传递的 `warp-workflows-types`、`uneval`） | `cloud_object_models:31`、`integration:46`、`app:229` | **保留** | `cloud_object_models` 删后少一条边；`app` 的本地 workflow 仍需要它 |
| `font-kit`、`notify-debouncer-full`、`vte`、`winit`、`cosmic-text`、`dwrote`、`difflib`、`email_address` | 见 [02 §5.2](02-现状审计.md#s5-2) | **保留** | 都是上游 OSS 库的 warpdotdev fork，与云无关 |

<a id="s2-1"></a>
### 2.1 两条必须手工剥离的残留边

| # | 边 | 剥离方法 |
|---|---|---|
| E1 | `crates/warp_terminal` ← `session-sharing-protocol`（`crates/warp_terminal/Cargo.toml:49`） | 定位 `warp_terminal` 里引用该 crate 的类型，判断是"共享会话协议类型"（随删）还是"被复用的通用类型"（本地化重定义）。**这是 V1 的具体任务，不是 V0。** |
| E2 | `crates/persistence` ← `warp_multi_agent_api`（`crates/persistence/Cargo.toml:24`） | 同上。`persistence` 用它多半是 agent 相关表的 proto 类型；如果确实如此，随 [§11](#s11) 的"保留表声明"策略，这条边也可以**先留着**（proto 类型是纯数据结构，不产生网络行为）。 |

> **E2 的判断规则**：只要 `warp_multi_agent_api` 里没有任何 client/transport（纯 proto
> 类型），保留这条边**不违反 H1–H4 任何一条**，因为它进不了运行时。V1 决定是否为了
> "依赖树干净"而付出剥离成本。**验收标准**：`cargo tree -p warp --no-default-features
> --features local_only | rg 'warp_multi_agent_api'` 若仍命中，需人工确认它只提供类型。

---

<a id="s3"></a>
## 三、入口设计

<a id="s3-1"></a>
### 3.1 新增两个 bin：`term4u` 与 `term4u-tui`

**以 `oss.rs` 为模板**（[02 §4.6](02-现状审计.md#s4-6)）。

```toml
# app/Cargo.toml —— 新增，放在现有 [[bin]] 之前，并把 default-run 指向它
default-run = "term4u"

[[bin]]
name = "term4u"
path = "src/bin/term4u.rs"
test = false
```

```toml
# crates/warp_tui/Cargo.toml
default-run = "term4u-tui"

[[bin]]
name = "term4u-tui"
path = "src/bin/term4u.rs"
```

```rust
// app/src/bin/term4u.rs
#![cfg_attr(feature = "release_bundle", windows_subsystem = "windows")]

use anyhow::Result;
use warp_core::AppId;
use warp_core::channel::{Channel, ChannelConfig, ChannelState, ConnectivityMode};

fn main() -> Result<()> {
    let mut state = ChannelState::new(
        Channel::Offline,
        ChannelConfig {
            app_id: AppId::new("dev", "term4u", "Term4u"),   // ← V2 定稿，V0/V1 可暂留 warp 值
            logfile_name: "term4u.log".into(),
            connectivity: ConnectivityMode::Offline { allow_loopback: true },
            telemetry_config: None,
            crash_reporting_config: None,
            autoupdate_config: None,
            mcp_static_config: None,
        },
    );
    if cfg!(debug_assertions) {
        state = state.with_additional_features(warp_core::features::DEBUG_FLAGS);
    }
    ChannelState::set(state);
    warp::run()
}
```

> **禁止事项（调研原文明列，此处照单执行）**：
>
> ```rust
> server_root_url: ""                     // ✗
> server_root_url: "http://127.0.0.1:9"   // ✗
> server_root_url: "https://invalid.local"// ✗
> ```
>
> 理由：现有代码大量假设"URL 一定存在、一定可解析"。假地址会产生重试循环、后台
> 报错、DNS 查询、回退到其他地址、以及未来代码误用。**正确做法是让云配置在类型
> 层面不存在**，见 [§4](#s4)。

<a id="s3-2"></a>
### 3.2 删 8 个生成器 bin + 相关脚本

| 删除对象 | 数量 | 理由 |
|---|---|---|
| `app/src/bin/{stable,dev,preview,local}.rs` | 4 | 依赖私有 `warp_channel_config::load_config!`，运行时必 panic（[02 §4.5](02-现状审计.md#s4-5)） |
| `crates/warp_tui/src/bin/{stable,dev,preview,local}.rs` | 4 | 同上 |
| `app/Cargo.toml` `[[bin]]` 段 `:35-49`、`crates/warp_tui/Cargo.toml` `[[bin]]` 段 | — | 随之删 |
| `script/install_channel_config` | 1 | 只为安装那个私有生成器而存在 |
| `crates/warp_channel_config/` | 1 crate | 删完 8 个 bin 后无人引用 |
| `Channel::{Stable, Dev, Preview, Local}` 枚举变体 | — | **V1 再删**——`channel/state.rs:392-401` 的 `url_scheme()`、`paths.rs:97-111` 的目录名映射、`paths_tests.rs` 的断言都要同步改；V0 阶段留着不影响任何指标 |

<a id="s3-3"></a>
### 3.3 保留的 bin

| bin | 处置 |
|---|---|
| `warp-oss` / `warp-tui-oss` | **V0 保留**（作为"删改前后行为对照"的参照物），**V1 删除** |
| `integration`（`app/src/bin/integration.rs` 与 `crates/integration/src/bin/integration.rs`） | **保留**——它已经离线（[02 §4.7](02-现状审计.md#s4-7)），只需把 `192.0.2.0:9` 换成 `ConnectivityMode::Offline` |

---

<a id="s4"></a>
## 四、ConnectivityMode 与 Channel::Offline

<a id="s4-1"></a>
### 4.1 类型改造

```rust
// crates/warp_core/src/channel/config.rs

/// 连通性模式。离线是一个**状态**，不是一组值 —— 这是整套设计的第一性原则。
#[derive(Debug, Deserialize, Serialize)]
pub enum ConnectivityMode {
    /// 完全离线。云配置在类型层面不存在，不可能被误读为"空字符串"或"某个地址"。
    Offline {
        /// 是否允许 loopback（本地 MCP、本地 LSP、OAuth loopback 回调需要）。
        allow_loopback: bool,
    },
    /// 连云。仅在 `#[cfg(not(feature = "offline_hard"))]` 下可构造。
    #[cfg(not(feature = "offline_hard"))]
    Cloud {
        server: WarpServerConfig,
        oz: OzConfig,
    },
}

pub struct ChannelConfig {
    pub app_id: AppId,
    pub logfile_name: Cow<'static, str>,
    pub connectivity: ConnectivityMode,          // ← 取代 server_config + oz_config
    pub telemetry_config: Option<TelemetryConfig>,
    pub autoupdate_config: Option<AutoupdateConfig>,
    pub crash_reporting_config: Option<CrashReportingConfig>,
    pub mcp_static_config: Option<McpStaticConfig>,
}
```

**为什么用 enum 而不是 `Option<WarpServerConfig>`**（调研原文列了两种，此处选前者）：
`Option` 会让每个调用点自己决定 `None` 怎么办，而现有代码里已经有 `unwrap_or_default()`
和 `expect(...)` 的习惯（例如 `state.rs:214` 的 `.unwrap_or_default()`、`:296` 的
`.expect("Server root URL should be valid")`）。enum + `#[cfg]` 让"云配置"在
`offline_hard` 构建里**根本不存在这个类型**，误用会变成编译错误而不是运行时空串。

<a id="s4-2"></a>
### 4.2 九个访问器的改造

`ChannelState` 的九个云访问器（[02 §4.3](02-现状审计.md#s4-3)）
统一改为返回 `Result<_, OfflineError>`：

```rust
// crates/warp_core/src/channel/state.rs

#[derive(Debug, thiserror::Error)]
#[error("term4u is built offline-only; there is no {0}")]
pub struct OfflineError(&'static str);

impl ChannelState {
    pub fn server_root_url() -> Result<Cow<'static, str>, OfflineError> {
        cfg_if::cfg_if! {
            if #[cfg(feature = "test-util")] {
                Ok(Cow::Owned(MOCK_SERVER_URL.clone()))          // :276 —— 测试短路保持不变
            } else if #[cfg(feature = "offline_hard")] {
                Err(OfflineError("server root URL"))
            } else {
                Ok(CHANNEL_STATE.lock().config.connectivity.server()?.server_root_url.clone())
            }
        }
    }
    // ws_server_url / rtc_http_url / session_sharing_server_url / oz_root_url /
    // workload_audience_url / firebase_api_key / iap_config / server_root_domain 同构
}
```

**`server_root_domain()`（`:296`）必须特别处理**——它现在是
`.expect("Server root URL should be valid")`，在离线下会直接 panic。改成
`Result` 后所有调用点被编译器逼着处理。

<a id="s4-3"></a>
### 4.3 测试侧成本 = 4 处

| 位置 | 改动 |
|---|---|
| `state.rs:59` `mock_server()` | 不变（`#[cfg(feature = "test-util")]`） |
| `state.rs:249` `rtc_http_url` 的 test-util 分支 | 包一层 `Ok(...)` |
| `state.rs:262` `session_sharing_server_url` 的 test-util 分支 | 同上 |
| `state.rs:276` `server_root_url` 的 test-util 分支 | 同上 |

**`test-util` 分支的优先级必须高于 `offline_hard`**（上面 `cfg_if` 的顺序），
否则 899 个测试文件里所有依赖 mockito 的都会拿到 `Err`。

<a id="s4-4"></a>
### 4.4 `Channel::Offline`

新增枚举变体，并在四处同步：

| 位置 | 改动 |
|---|---|
| `crates/warp_core/src/channel/mod.rs` | 加 `Offline` 变体 |
| `state.rs:392-401` `url_scheme()` | `Offline => "term4u"` |
| `crates/warp_core/src/paths.rs:97-111` `macos_config_dir_name_for()` | `Offline => ".term4u"` |
| `paths.rs:51` `warp_home_config_dir_name()` / `:364` `project_dirs_for_app_id()` | 同步；注意 linux 的名字改写规则（`"Warp"→"Warp-Terminal"` 等）对 `Term4u` 不适用，要加分支 |
| `crates/warpui/src/browser.rs:20-21` scheme 白名单 + `browser_tests.rs:23-26` | 加 `"term4u"` |

> **注意**：这一组改动会**改变数据目录**。因此它属于 [05 §6](05-阶段1-仓库与品牌.md#s6)
> 的"六处运行时身份"，必须与 AppId 改名**同批**做，时机是 V2 / M2 之前。
> V0/V1 阶段可以让 `Channel::Offline` 暂时复用 `.warp-oss` 目录名。

---

<a id="s5"></a>
## 五、initialize_app 三分

<a id="s5-1"></a>
### 5.1 现状

```
app/src/lib.rs            3,190 行
├── pub fn run()          :744-843    （100 行）
├── fn run_internal()     :979-1363   （385 行）
├── fn initialize_app()   :1456-2606  （1,151 行）  ← 本地终端 / GUI / 认证 / ServerApi /
│                                                     遥测 / AI / Drive / 共享 / 更新全混在一起
└── fn app_callbacks()    :2608-2936  （329 行）
```

<a id="s5-2"></a>
### 5.2 三分方案

**不要**在这个 1,151 行的函数里撒几十个 `#[cfg(not(feature = "offline_hard"))]`——
那会让它变成一个谁也读不懂、合并必冲突的东西。拆成三个函数：

```rust
// app/src/lib.rs
pub(crate) fn initialize_app(/* … */) {
    initialize_common_app(/* … */);

    #[cfg(feature = "offline_hard")]
    initialize_local_app(/* … */);

    #[cfg(not(feature = "offline_hard"))]
    initialize_cloud_app(/* … */);
}
```

| 函数 | 内容 |
|---|---|
| `initialize_common_app` | 本地 Settings、本地 SQLite、PTY、Terminal、Tabs / Panes、Editor、File Tree、Local Shell、Themes、Keybindings、Local Workflows、本地文件搜索、本地 LSP |
| `initialize_local_app` | `LocalAuthStateProvider`、`OfflineServerApiProvider`（[§8](#s8)）、`LocalPrivacyPolicy`（[§10](#s10)）、本地 MCP（`app/src/ai/mcp/file_mcp_watcher.rs` / `file_based_manager.rs`） |
| `initialize_cloud_app` | `ServerApiProvider`、`AuthManager`、`UserWorkspaces`、`CloudModel` 同步、`TelemetryCollector`、Sentry、Warp Agent、Ambient Agents、Cloud Drive、Shared Sessions、Autoupdate、IAP、Cloud MCP、Voice Transcriber、Server Experiments |

**`offline_hard` 构建里 `initialize_cloud_app` 根本不编译。**

<a id="s5-3"></a>
### 5.3 `app_callbacks` 的四个出口

`app_callbacks()`（`:2608-2936`）注册了四个生命周期回调，全部有遥测出口：

| 回调 | 现行为 | 离线行为 |
|---|---|---|
| `on_become_active` | `record_app_focus` | 删（宏已空，但回调本身也应删以省一次无用调用） |
| `on_resigned_active` | `record_app_blur` | 同上 |
| `on_will_terminate` | `flush_telemetry_events_for_shutdown` | **删** —— 这是"退出时写未发送事件到本地、下次启动重发"的入口，必须删干净 |
| `on_should_close` | `TelemetryEvent` | 删 |

> **`on_will_terminate` 是唯一会在磁盘上留下"待发遥测文件"的路径**，因此它是
> [07 §12](07-测试与验证策略.md#s7-12) 里"无待发遥测文件"这条验收的对应改动。

<a id="s5-4"></a>
### 5.4 拆分顺序（重要）

拆分是一次**纯机械的搬运**，但必须在删除之前做，理由：

1. 先拆分 → `initialize_cloud_app` 里的东西整块不编译 → 后续删除是"删已经不编译的代码"，
   风险最低；
2. 先删除 → 在 1,151 行里找哪些行该删，边删边坏，无法二分定位。

因此里程碑顺序是 **M3（三分）→ M4（删叶子）**，见 [08 章](08-实施顺序与里程碑.md)。

---

<a id="s6"></a>
## 六、feature 重构

<a id="s6-1"></a>
### 6.1 三个原则

1. **`offline_hard` 是跨 crate 的**——只在 `app` 里定义没有意义，因为守卫在
   `http_client` / `websocket` 里。
2. **`local_only` 是加法定义**——从现有 202 项 `default` 做减法必然漏，而且每次
   上游加 feature 就会漏一次。
3. **`gui` 必须与 `voice_input` 解耦**。

<a id="s6-2"></a>
### 6.2 `offline_hard`

```toml
# crates/http_client/Cargo.toml
[features]
offline_hard = []

# crates/websocket/Cargo.toml
[features]
offline_hard = []

# crates/warp_core/Cargo.toml
[features]
offline_hard = ["http_client/offline_hard", "websocket/offline_hard"]

# app/Cargo.toml
[features]
offline_hard = [
    "warp_core/offline_hard",
    "http_client/offline_hard",
    "websocket/offline_hard",
]
```

> **注意 feature unification 的陷阱**：Cargo 会把同一个 crate 的 feature 在整个
> 构建图里做并集。这里是**有利的**——只要有任何一条路径开了
> `http_client/offline_hard`，守卫就生效。但反过来也意味着：**只要有任何一个
> workspace 成员在某次构建里没开它，那次构建就没有守卫。**
> 因此 [07 §10](07-测试与验证策略.md#s7-10) 的编译矩阵里，
> "默认构建仍须可编译"这一项**不带守卫**，不能用它做离线验收。

<a id="s6-3"></a>
### 6.3 `local_only`

```toml
# app/Cargo.toml
[features]
local_only = [
    "offline_hard",
    # 本地能力（显式列出，不从 default 减）
    "local_tty",
    "local_fs",
    "settings_file",
    "file_tree",
    "tabbed_editor_view",
    "vim_code_editor",
    "code_find_replace",
    "global_search",
    "command_palette_file_search",
    "kitty_keyboard_protocol",
    "kitty_images",
    "osc_hyperlinks",
    "ligatures",
    "shell_selector",
    "minimalist_ui",
    "full_screen_zen_mode",
    "rect_selection",
    "ui_zoom",
    "undo_closed_panes",
    "vertical_tabs",
    "tab_configs",
    "grouped_tabs",
    "pinned_tabs",
    "file_based_mcp",
    "mcp_server",
    "classic_completions",
    "workflow_aliases",
    "am_workflows",
    # …按需逐项增加，每加一项要能说出它为什么是本地能力
]
```

构建命令：

```bash
cargo build -p warp --bin term4u --no-default-features --features local_only
```

> **`local_tty` / `local_fs` 的特殊性（[02 §6.5](02-现状审计.md#s6-5)）**：
> `app/build.rs:231-235` 已经给 `app` 自己打了这两个 `cfg`，但**不会**传播到依赖侧
> （`ai/local_fs`、`lsp/local_fs`、`persistence/local_fs`、`repo_metadata/local_fs`、
> `warp_core/local_fs`、`warp_terminal/local_fs`、`warp_terminal/local_tty`）。
> 把它们写进 `local_only` 是**必须的**（这才会打开依赖侧），但不要以为这是"新开了
> 一个开关"——`app` 侧本来就是开的。
>
> 尤其注意 `crates/persistence/src/lib.rs:4-6` 的 `MIGRATIONS` 常量 gated on
> `feature = "local_fs"`：**如果 `persistence/local_fs` 没打开，本地数据库根本没有迁移。**

<a id="s6-4"></a>
### 6.4 `gui` 解耦

```toml
# app/Cargo.toml:741 —— 现状
gui = ["voice_input"]

# 改为
gui = []
voice_input = ["dep:voice_input"]
cloud_voice_input = ["voice_input"]     # 只有它才会注册 ServerVoiceTranscriber
```

`local_only` 不启用 `cloud_voice_input`。若将来要本地语音，实现一个
`LocalVoiceTranscriber`（如调用本机 Whisper），不经过 `ServerApi`。

<a id="s6-5"></a>
### 6.5 需要显式确认"不在 `local_only` 内"的 default 项

以下都在现有 `default`（202 项）里，且是云功能，**加法定义天然不会带上它们**——
但要在代码评审时逐条确认没有被间接带入：

`agent_mode`、`viewing_shared_sessions`、`shared_with_me`、`session_sharing_acls`、
`agent_shared_sessions`、`shared_session_long_running_commands`、
`global_ai_analytics_collection`、`mcp_oauth`、`api_key_management`、`web_search_ui`、
`web_fetch_ui`、`cloud_environments`、`cloud_conversations`、`cloud_mode`、
`cloud_mode_from_local_session`、`cloud_mode_image_context`、`cloud_mode_input_v2`、
`cloud_mode_setup_v2`、`cloud_runners`、`cloud_agent_runners`、`conversation_artifacts`、
`conversation_api`、`oz_handoff`、`oz_platform_skills`、`oz_identity_federation`、
`oz_changelog_updates`、`oz_launch_modal`、`handoff_local_cloud`、`handoff_cloud_cloud`、
`remote_codebase_indexing`（→ `full_source_code_embedding`）、`factory_mcp`、
`well_known_mcp_ids`、`warp_managed_secrets`、`team_api_keys`、
`ambient_agents_command_line`、`ambient_agents_image_upload`、`scheduled_ambient_agents`、
`sync_ambient_plans`、`ambient_agents_rtc`、`agent_mode_computer_use`、
`background_computer_use`、`agent_view`（+ 8 个依赖它的 `agent_view_*` / `inline_*`）、
`bundled_skills`、`autoupdate_ui_revamp`、`account_first_onboarding`、
`loginless_conversion`、`usage_based_pricing`、`billing_and_usage_page_v2`、
`skip_firebase_anonymous_user`、`agent_harness`、`hoa_*`(4)、`codex_notifications`、
`codex_plugin`、`supergrok`、`gemini_enterprise`、`solo_user_byok`。

> **验收命令**（[07 §10](07-测试与验证策略.md#s7-10)）：
> ```bash
> cargo tree -p warp --no-default-features --features local_only -e features \
>   | rg 'agent_mode|cloud_|shared_|ambient_|oz_|computer_use|autoupdate|bundled_skills'
> ```
> 理想结果为空。

---

<a id="s7"></a>
## 七、网络硬拒绝：在 socket 对端地址层

<a id="s7-1"></a>
### 7.1 拦截层的选择不是权衡，是唯一解

| 方案 | 结果 |
|---|---|
| 在 target URI / Host 层拦截 | **误杀** `crates/websocket/src/proxy_tests.rs` 的 3 个测试（`:332-367`、`:369-402`、`:404-433`）——它们用 loopback 代理连非 loopback 的**目标 URI** |
| 在 **socket 对端地址**层拦截 | 这 3 个测试**完全不受影响**——`connect_via_proxy`（`crates/websocket/src/proxy.rs:69`）唯一的真实 socket 操作是 `TcpStream::connect((&*proxy.host, proxy.port))`（`proxy.rs:81`），即 127.0.0.1 |

详见 [02 §7.12](02-现状审计.md#s7-12)。

<a id="s7-2"></a>
### 7.2 守卫的位置

| 出口 | 文件:行 | 说明 |
|---|---|---|
| HTTP / SSE 主路径 | `crates/http_client/src/lib.rs:370-400` `execute_inner` | `self.wrapped.execute(request)` 在 `:385`(wasm) / `:391`(native) |
| **SSE 旁路** | `crates/http_client/src/lib.rs:499-~570` `eventsource` | **绕过 `execute_inner`**，`:504` / `:525` 直接调 `self.wrapped.eventsource()` —— **必须单独加守卫** |
| WebSocket 直连 | `crates/websocket/src/native.rs:49` `connect_async_with_tls_connector` | |
| WebSocket 走代理 | `crates/websocket/src/native.rs:43` → `proxy::connect_via_proxy` → `crates/websocket/src/proxy.rs:81` `TcpStream::connect` | **守卫加在 `proxy.rs:81`**，而不是 `native.rs:32` 的入口——这样代理本身是 loopback 就放行 |

<a id="s7-3"></a>
### 7.3 守卫规则

```rust
// 建议放在 crates/http_client/src/offline_guard.rs，由 websocket 也复用
// （或独立成 crates-local/offline_guard，见 06 章的自研 crate 规划）

#[derive(Debug, thiserror::Error)]
#[error("offline build refused an outbound connection to {0}")]
pub struct OutboundRefused(pub String);

/// 唯一的判定入口。规则是**默认拒绝**：只有明确在白名单里的才放行。
#[cfg(feature = "offline_hard")]
pub fn check_peer(addr: &std::net::SocketAddr) -> Result<(), OutboundRefused> {
    let ip = addr.ip();
    let allowed = ip.is_loopback()          // 127.0.0.0/8, ::1
        || ip.is_unspecified();             // 0.0.0.0 / :: —— 仅用于本地 bind
    if allowed { Ok(()) } else { Err(OutboundRefused(addr.to_string())) }
}

#[cfg(not(feature = "offline_hard"))]
pub fn check_peer(_: &std::net::SocketAddr) -> Result<(), OutboundRefused> { Ok(()) }
```

| 目标 | 判定 | 理由 |
|---|---|---|
| `127.0.0.0/8`、`::1` | **放行** | 本地 MCP、本地 LSP、OAuth loopback 回调 |
| Unix domain socket | **放行**（不经过 `check_peer`，因为它没有 `SocketAddr`） | 本地 IPC |
| 其他一切 IP | **拒绝** | H3 / H4 |
| 私有网段 `10/8`、`192.168/16`、`172.16/12` | **拒绝** | term4u 是本机软件，不是内网客户端。若将来需要，加显式配置项，**不要**默认放行 |
| 域名（未解析） | **在解析后判定**——守卫必须看到最终的 `SocketAddr` | 这正是"socket 对端地址层"的含义 |

> **`is_unspecified()` 的放行是为 bind 服务的，不是为 connect**。如果实现时能区分
> bind 与 connect，connect 路径应当去掉这一条。

<a id="s7-4"></a>
### 7.4 DNS 也要管

守卫拦在 `connect` 上时，DNS 查询**已经发生了**。而 [07 §12](07-测试与验证策略.md#s7-12)
的合格标准是"**零 DNS 查询**"。因此还需要：

| 措施 | 位置 |
|---|---|
| `reqwest` 自定义 resolver：`offline_hard` 下对非 loopback 主机名直接返回解析失败，不发查询 | `http_client::Client::new()` 构造处（`crates/http_client/src/lib.rs:138` 起的 `impl Client`） |
| `websocket` 侧同理 | `crates/websocket/src/native.rs:36` 之前 |
| 兜底：hosts 白名单只含 `localhost` | 同上 |

**这是"零 DNS"与"连接失败"的区别所在**，也是为什么验收标准不能写成"请求失败"。

<a id="s7-5"></a>
### 7.5 守卫的测试清单

| 测试 | 断言 |
|---|---|
| `guard_refuses_non_loopback_http` | `Client::get("http://198.51.100.1/")` → `OutboundRefused` |
| `guard_allows_loopback_http` | 对本地 `mockito` server 的请求正常返回 |
| `guard_refuses_non_loopback_eventsource` | `RequestBuilder::eventsource()` 走非 loopback → `OutboundRefused`（**这条最容易漏**） |
| `guard_allows_loopback_eventsource` | 本地 SSE 正常 |
| `guard_refuses_non_loopback_websocket` | `websocket::connect("wss://198.51.100.1/")` → `OutboundRefused` |
| `guard_allows_loopback_proxy_to_remote_target` | **回归测试**：loopback 代理 + 非 loopback 目标 URI **必须放行**——保护 `proxy_tests.rs:332-433` 那三个测试的语义 |
| `guard_allows_unix_socket` | Unix socket 路径不经过 `check_peer` |
| `guard_makes_no_dns_query` | 对一个不存在的域名发请求，用本地 DNS 观测确认零查询（可用 `dtruss`/`strace` 或注入的 resolver 计数器） |

> 用 `198.51.100.0/24`（TEST-NET-2）作测试目标，与上游 harness 用的 `192.0.2.0/24`
> （TEST-NET-1）区分开，避免混淆。

---

<a id="s8"></a>
## 八、ServerApi 空实现（null object）而非删除

<a id="s8-1"></a>
### 8.1 为什么不删

调研原文建议"`offline_hard` 下不注册 `ServerApiProvider`"，并明确不建议做"指向假
地址的 ServerApi"。**本设计采纳前半句、修正后半句**：不指向假地址是对的，但
"不注册"会带来两个成本：

| 成本 | 量化 |
|---|---|
| 57 个测试文件直接调用 `ServerApiProvider::new_for_test()`（`app/src/server/server_api.rs:1409`） | 全部要改 |
| **决策 D4 要求"接不上云端的 UI 外壳先保留"** | UI 拿不到 provider 会 panic 或需要大改 |

**null object 同时解决这两件事**：类型还在、DI 还能注册、UI 拿得到、方法一律返回
明确的离线错误。

<a id="s8-2"></a>
### 8.2 空实现的形状（已实测：13 个 trait / 178 个方法）

`ServerApiProvider` 已经把能力切成了 13 个 `Arc<dyn XxxClient>`
（`app/src/server/server_api.rs:1424-1480`）——这是空实现最好的着力点：

| trait | 定义位置 | 方法数 |
|---|---|---|
| `AIClient` | `app/src/server/server_api/ai.rs:1183-1570` | **70** |
| `ObjectClient` | `crates/cloud_object_client/src/lib.rs:201-372` | 30 |
| `AuthClient` | `crates/warp_server_auth`→`crates/warp_server_client/src/auth/mod.rs:84-168` | 18 |
| `TeamClient` | `app/src/server/server_api/team.rs:72-164` | 16 |
| `HarnessSupportClient` | `app/src/server/server_api/harness_support.rs:299-371` | 13 |
| `ManagedSecretsClient` | `crates/managed_secrets/src/client.rs:50-96` | 8 |
| `IntegrationsClient` | `app/src/server/server_api/integrations.rs:57-137` | 7 |
| `WorkspaceClient` | `app/src/server/server_api/workspace.rs:46-71` | 5 |
| `BlockClient` | `app/src/server/server_api/block.rs:28-47` | 4 |
| `FactoryClient` | `app/src/server/server_api/factory.rs:33-45` | 3 |
| `ReferralsClient` | `app/src/server/server_api/referral.rs:30-36` | 2 |
| `ManagedMcpClient` | `app/src/server/server_api/managed_mcp.rs:21-28` | 1 |
| `TuiOnboardingClient` | `app/src/server/server_api/tui_onboarding.rs:22-24` | 1 |
| **合计** | | **178** |

```rust
// app/src/server/offline_api.rs（新增）

/// 离线构建下的 ServerApiProvider。所有方法返回 `OfflineError`，不发任何请求。
pub struct OfflineServerApiProvider;

impl OfflineServerApiProvider {
    pub fn get_ai_client(&self) -> Arc<dyn AIClient> { Arc::new(OfflineAIClient) }
    // …13 个 getter 同构
}

struct OfflineAIClient;

#[async_trait]
impl AIClient for OfflineAIClient {
    // 70 个方法。绝大多数形如：
    async fn some_call(&self, /* … */) -> Result<T> { Err(OfflineError("AI").into()) }
    // 返回集合的方法返回空集合而不是错误，避免 UI 反复重试：
    async fn list_something(&self) -> Result<Vec<T>> { Ok(Vec::new()) }
}
```

<a id="s8-3"></a>
### 8.3 三条实现纪律

1. **返回错误 vs 返回空**：查询类（list / get）返回**空集合或 `None`**，
   写入类（create / update / delete / send）返回 `Err(OfflineError)`。
   理由：查询返回错误会让 UI 显示"加载失败"并重试；返回空会让 UI 显示"没有内容"，
   这才是离线的正确语义。
2. **绝不 `todo!()` / `unimplemented!()`**——那是 panic，会在用户点到某个按钮时炸掉。
3. **绝不发请求**——即使是"看起来无害"的健康检查。空实现里不能出现 `http_client`。

<a id="s8-4"></a>
### 8.4 UI 降级要求（D4 的落地）

| UI | 降级行为 |
|---|---|
| 登录 / 账号页 | 显示"term4u 是纯本地构建，没有账号"，**不显示登录按钮** |
| Agent 视图（若保留 blocklist） | 显示"未配置本地 Agent 运行时"；输入框禁用；**不重试** |
| Warp Drive / 团队页 | 从菜单里移除（不是灰掉——灰掉的按钮还是会被点） |
| 设置页的隐私区 | 显示 [§10](#s10) 的固定值，**只读** |
| 更新检查 | 从菜单移除 |

> **"从菜单移除"优于"禁用"**：调研 §十三.3 列了一批"会打开浏览器/访问外部站点"的
> 菜单项（Privacy Policy、Data Management、Docs、GitHub Issue、Drive Web Link、
> Oz Dashboard）。这些不上传数据，但会误触。删菜单是唯一彻底的做法。

<a id="s8-5"></a>
### 8.5 V1 的再评估

V0 完成后重新测量：`OfflineServerApiProvider` 的 178 个方法里，实际被 UI 调到的有
几个？没被调到的说明对应 UI 已经删干净了，那部分 trait 可以整块删。**这是 V1 判断
"ServerApi 能不能真删"的量化依据**，而不是靠感觉。

---

<a id="s9"></a>
## 九、遥测：保留 trait + 宏 shim，只删 collector 与传输

<a id="s9-1"></a>
### 9.1 成本对比（这是本章最容易做错的决策）

| 方案 | 改动量 |
|---|---|
| 删 `crates/warp_core/src/telemetry.rs` | **212 个文件 / 888 处**（[02 §7.10](02-现状审计.md#s7-10)） |
| **保留为 no-op shim** | **1 个文件** |

<a id="s9-2"></a>
### 9.2 shim 的形状

```rust
// crates/warp_core/src/telemetry.rs

// trait 与 register 宏原样保留（:20 TelemetryEvent, :62 register_telemetry_event!,
// :81 TelemetryEventDesc）—— 它们只是类型与元数据，不产生行为。

#[cfg(feature = "offline_hard")]
#[macro_export]
macro_rules! send_telemetry_from_ctx {
    ($($tt:tt)*) => {{
        // 故意求值所有参数一次，避免 unused 变量警告导致 clippy 失败。
        let _ = ($($tt)*);
    }};
}
// send_telemetry_from_app_ctx! / send_telemetry_sync_from_ctx! /
// send_telemetry_sync_from_app_ctx! / record_telemetry_from_ctx! /
// record_telemetry_on_executor! 同构（:147、:176 及其余四个）
```

> **`let _ = (...)` 而不是 `{}`**：宏体直接空掉会让调用点的局部变量变成未使用，
> 而 presubmit 的 clippy 是 `-D warnings`（[02 §7.9](02-现状审计.md#s7-9)）。
> 这一个细节能省掉几百个 `#[allow(unused)]`。
> **实现时须逐个宏核对参数形态**（有的宏接 `ctx, Event{..}`，有的接表达式列表），
> 保证 `let _ = (...)` 语法上成立；不成立的用 `{ let _ = &$ctx; }` 之类的变体。

<a id="s9-3"></a>
### 9.3 真正要删的

| 对象 | 位置 |
|---|---|
| `TelemetryCollector` 及其注册 | `app/src/lib.rs` 的 `initialize_app`（随 [§5](#s5) 进 `initialize_cloud_app`） |
| Rudderstack 传输 | `app/src/server/telemetry/mod.rs`（取队列 → 分 UGC / 非 UGC → POST → 退出时落盘 → 启动重发） |
| 四个定时器 | 30s 刷事件 / 60s 记 Active Usage / 退出保存 / 启动重发 |
| `TelemetryApi` | `app/src/server/server_api.rs:442`（`ServerApi` 的字段，代码里已有 TODO 要把它挪走） |
| `crates/warpui_core/src/telemetry/event_store.rs` 的落盘逻辑 | **MIT 岛内**，改动需加双版权行 |
| Rudder SDK 依赖 | `Cargo.toml`（MIT，随之删） |

<a id="s9-4"></a>
### 9.4 验收标准的修正

> **不能用 `rg send_telemetry | wc -l == 0` 作为验收**——按本方案它永远不会是 0
> （宏点还在 193 个文件里）。

正确的验收（[07 §6](07-测试与验证策略.md#s7-6)）：

```bash
# 1. collector 与传输不存在
test ! -d app/src/server/telemetry && echo OK
rg -n 'TelemetryCollector|initialize_telemetry_collection|rudderstack' app crates && echo FAIL || echo OK

# 2. 二进制里没有传输相关字符串
strings target/release/term4u | rg -i 'rudder|sentry|otlp|opentelemetry' && echo FAIL || echo OK

# 3. 退出后不产生待发文件
#    （运行一次再退出，检查数据目录）
find "$TERM4U_DATA_DIR" -name '*telemetry*' | grep . && echo FAIL || echo OK
```

---

<a id="s10"></a>
## 十、LocalPrivacyPolicy 固化

<a id="s10-1"></a>
### 10.1 现状：默认全开，且能被云端翻回去

```
app/src/settings/privacy.rs        35,535 字节
app/src/settings_view/privacy_page.rs  76,130 字节
app/src/ai/blocklist/telemetry_banner.rs  8,274 字节
```

| 设置 | 默认值 | 位置 |
|---|---|---|
| `is_telemetry_enabled` | **`true`** | `app/src/settings/privacy.rs:87-95`（`toml_path = "privacy.telemetry_enabled"`） |
| `is_crash_reporting_enabled` | **`true`** | `:98-106`（`toml_path = "privacy.crash_reporting_enabled"`） |
| `is_cloud_conversation_storage_enabled` | **`true`** | `:109-117`（`toml_path = "agents.cloud_conversation_storage_enabled"`） |

云端同步路径：

| 函数 | 位置 |
|---|---|
| `fetch_or_update_settings` | `app/src/settings/privacy.rs:376`，由 `app/src/auth/auth_manager.rs:510` 在登录后调用 |
| `update_server_with_local_settings` | `app/src/settings/privacy.rs:684`，被 `:403` / `:409` 调用 |

代码里还存在**组织强制开启**与 **Agent Analytics 实验覆盖本地关闭值**的路径
（`privacy.rs:160` 附近的注释直言："If false, we fall back to the user's settings"）。

<a id="s10-2"></a>
### 10.2 改造

```rust
// app/src/settings/local_privacy_policy.rs（新增）

/// 本地隐私策略。这些是**常量**，不是设置项 —— term4u 不存在"以后服务器或实验
/// 可以重新开启"的路径。
pub struct LocalPrivacyPolicy;

impl LocalPrivacyPolicy {
    pub const TELEMETRY_ENABLED: bool = false;
    pub const CRASH_REPORTING_ENABLED: bool = false;
    pub const CLOUD_STORAGE_ENABLED: bool = false;
}
```

| 动作 | 对象 |
|---|---|
| **删** | `fetch_or_update_settings`（`privacy.rs:376`）、`update_server_with_local_settings`（`:684`）及其两个调用点（`:403`、`:409`） |
| **删** | 组织强制路径与实验覆盖路径 |
| **改** | 三个 setting 的 `default` 改为 `false`，且**去掉 setter**（改成只读常量） |
| **改** | `settings_view/privacy_page.rs` 改为只读展示三个 `false` + 一句"本地构建，不可更改" |
| **删** | `app/src/ai/blocklist/telemetry_banner.rs`（本地构建没有要提示的遥测） |

> **不要保留"可变开关"**。一个可以被打开的开关，就是一条可以被打开的外发路径；
> 而 term4u 的承诺是"编译期就没有这条路径"。

---

<a id="s11"></a>
## 十一、持久化：迁移不删

<a id="s11-1"></a>
### 11.1 结论

**141 个迁移、50 张表、17 张云表——一个都不删。**

<a id="s11-2"></a>
### 11.2 依据

| 论点 | 证据 |
|---|---|
| 删迁移会破坏旧 DB 的可加载性 | 迁移是有序链，删中间任何一环，`run_pending_migrations` 对已有 DB 就会失配 |
| 收益为零 | 表是惰性的：不写就没有行，不占内存、不占 CPU、不产生网络 |
| **删表声明的连带成本远超预期** | 实测：删除集**之外**仍有 **119 个文件 / 474 处**引用这 17 张云表（[02 §7.15](02-现状审计.md#s7-15)）。删声明会连带逼你删掉 `app/src/workspaces/`、`app/src/settings_view/teams_page.rs` 等一整批模块 |
| 没有迁移测试可依靠 | 全仓库**没有**迁移/schema 测试；迁移只在 `app/src/persistence/agent_tests.rs:10-11`、`block_list_tests.rs:26-27` 里作为 fixture 被间接执行 |

> **修正**：前序计划写"只删 `schema.rs` 表声明与 model 类型 → 影响 3 文件 37 处"。
> 实测存活侧 119 文件 / 474 处。**结论从"删声明"改为"连声明也不删"。**

<a id="s11-3"></a>
### 11.3 那删什么

| 删 | 不删 |
|---|---|
| 写入这些表的**云同步代码**（`SyncQueue`、`UpdateManager`、`Listener`、`CloudPreferencesSyncer`） | 表声明（`crates/persistence/src/schema.rs`） |
| 读取这些表的**云 UI**（Drive 视图、团队页——随 §1.4 删） | Rust model 类型（`crates/persistence/src/model.rs`） |
| — | 141 个迁移 |

<a id="s11-4"></a>
### 11.4 验收

```bash
# 用改造前的 warp-oss 建一个 DB，改造后用 term4u 打开，必须能正常启动且历史完整
```

写成可执行形式见 [07 §9](07-测试与验证策略.md#s7-9)。

---

<a id="s12"></a>
## 十二、待定：`app/src/ai/blocklist` 的三条路线（V1 决策点）

<a id="s12-1"></a>
### 12.1 规模

| 指标 | 值 |
|---|---|
| 源文件 / 源码行 | 149 / 98,892 |
| 测试文件 / 测试行 | 67 / 32,047 |
| 合计 | 216 文件 / 130,939 行 |
| 引用删除集模块的文件 / 处 | **145 / 525** |
| 其中属于类 B（编译断裂）的测试文件 | **43**（全仓库类 B 共 151，占 28%） |

它是 Warp 内置 Agent 的会话、工具调用、命令执行、文件编辑、Computer Use 的主要
UI 与执行层。

<a id="s12-2"></a>
### 12.2 三条路线

| # | 路线 | 工作量 | 得到什么 | 失去什么 |
|---|---|---|---|---|
| **A** | **整块删除** | 删 216 文件 / 130,939 行；同时删掉 43 个类 B 测试文件的修复工作 | 依赖树最干净；`cargo tree` 黑名单最容易清空 | 失去 Agent UI 外壳 —— **与决策 D4 冲突**；阶段 2 要接本地 Agent 时得从零写 UI |
| **B**（**V0 采用**） | **编译保留 + ServerApi 空实现降级** | 修 43 个类 B 测试文件；实现 [§8](#s8) 的 178 个方法 | 满足 D4；阶段 2 接 `agent_protocol` 时 UI 现成 | 依赖树里留着 AI 相关类型；98,892 行不跑的代码要过 clippy |
| **C** | **保留 UI、剥离云、接本地 Runtime** | B 的全部 + 实现 `agent_protocol` / `local_agent_bridge` | 一步到位 | **属于阶段 2 的工作量**，放进阶段 1 会让 V0 无限期推迟 |

<a id="s12-3"></a>
### 12.3 V0 的决定与 V1 的判据

**V0 采用路线 B。** 理由：它是唯一同时满足 D4（保 UI 外壳）与 V0 目标（尽快可用）的选项。

**V1 重新决策时用这三个量化判据**：

1. `OfflineServerApiProvider` 的 178 个方法里，被 blocklist 实际调到的有几个？
   （少 → 说明 UI 已经基本空转，倾向 A）
2. 43 个类 B 测试文件修完后，其中有多少是"只改 import"的机械修复，多少需要重写断言？
   （后者多 → 维护成本高，倾向 A）
3. 阶段 2 的 `agent_protocol` 设计（[06 §6](06-阶段2-完整MIT路线.md#s6)）
   是否真的能复用这套 UI？如果 ACP 的消息模型与 blocklist 的 `action_model` 差异过大，
   保留就没有意义（倾向 A）。

<a id="s12-4"></a>
### 12.4 无论走哪条，V0 都要删的 blocklist 子模块

Computer Use / 录屏 / Artifact 上传是明确无争议的删除项（调研 §六）：

```
app/src/ai/blocklist/action_model/execute/request_computer_use.rs
app/src/ai/blocklist/action_model/execute/use_computer.rs
app/src/ai/blocklist/action_model/execute/start_recording.rs
app/src/ai/blocklist/action_model/execute/stop_recording.rs
app/src/ai/blocklist/action_model/recording_controller.rs
app/src/ai/blocklist/action_model/recording_finalize.rs
app/src/ai/blocklist/action_model/recording_telemetry.rs
app/src/ai/agent_sdk/artifact_upload.rs          （随 agent_sdk 整块删）
crates/computer_use/                              （38 文件 / 10,468 行）
```

对应 feature：`agent_mode_computer_use`（default 内，`:625`）、
`background_computer_use`（default 内，`:626`）、`conversation_artifacts`（default 内，`:630`）。

录屏模块会录制窗口或整屏、生成 MP4 与缩略图、上传为云端 Artifact、上传后删除本地
文件——这是全仓库隐私影响最大的单一模块，**没有任何理由保留**。
