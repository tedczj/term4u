# M0 手工场景基线

基线 commit：`b5f4f0f636a1c9b6076e5bac9e2d33d26591ad70`。

本机只有 `/Library/Developer/CommandLineTools`，`xcrun` 找不到 `metal`/`metallib`，因此
`warp-oss` 无法产出可运行 GUI 二进制。以下 13 项均记录为**基础设施未执行**，不是通过；
原始失败见 `build-first.log`。在具备完整 Xcode 的 macOS runner 上必须补跑并回填行为、
`lsof` 与 DNS 快照。

| 场景 | 状态 | 行为 / lsof / DNS 证据 |
|---|---|---|
| S1 启动应用 | 未执行 | Metal 编译器缺失，构建退出 101 |
| S2 打开/关闭多个标签页 | 未执行 | 同上 |
| S3 分栏 | 未执行 | 同上 |
| S4 运行命令 | 未执行 | 同上 |
| S5 打开文件树 | 未执行 | 同上 |
| S6 预览文本 | 未执行 | 同上 |
| S7 打开设置 | 未执行 | 同上 |
| S8 窗口失焦/聚焦 | 未执行 | 同上 |
| S9 睡眠/唤醒 | 未执行 | 同上 |
| S10 关闭窗口 | 未执行 | 同上 |
| S11 退出应用 | 未执行 | 同上 |
| S12 崩溃测试构建 | 未执行 | 同上 |
| S13 空闲十分钟 | 未执行 | 同上 |
