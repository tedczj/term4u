# 08 · 实施顺序与里程碑

> 更新：2026-09-13；静态复核源码 `eef716cd69b84e0676eb90ad3ee776397aa6b515`。
> 本章仅维护阶段状态与范围。当前施工任务、顺序及验收以
> [11 · 本地化收敛施工单](11-本地化收敛施工单.md) 为唯一入口。
> R1/R2 已有批次验收，R3–R6 未关闭；历史 PASS 不直接继承到最新源码。

<a id="toc"></a>
## 目录

- [当前执行状态](#s0)
- [阶段关系](#s1)
- [身份与数据边界](#s2)
- [M0–M4：历史实现与遗留验收](#m0)
- [M5：本地功能与入口收敛](#m5)
- [M6：依赖和 V1 验收](#m6)
- [M7：后续品牌与发布](#m7)
- [风险登记](#s3)

<a id="s0"></a>
## 零、当前执行状态

当前代码已超过 `948d475b` 的大规模删除 WIP：`d3fd6fdc` 完成本地运行时 R1/R2 收敛并归档
批次验收，`eef716cd` 又修复 GUI 补全、提示符、clear 和焦点。**不能再写成“尚未恢复本地可用性”，
也不能由这些结果推出完整 V1 已验收。**

| 里程碑 | 当前判断 | 后续归属 |
|---|---|---|
| M0 | 原始基线保留；R2 已运行指定旧版生成真实 DB 样本，完成迁移/写入/重启验证；不因此补写所有历史 GUI 场景为通过 | R6 复用样本并回归；未覆盖的历史场景或替代批准仍按原范围记录 |
| M1 | 网络守卫已实现；R0 指定 debug 产物的静默网络、DNS 和完整进程树归因已有通过记录 | R6 对最终候选及实际产物重新验证，补 release/bundle、S1–S13 和第二道防线证据 |
| M2 | GUI/TUI 入口、local_only、运行时身份已落地；R1/R2 和最新 GUI 增量有构建/启动记录 | R6 关闭完整构建组合、产物和身份/路径矩阵 |
| M3 | 本地初始化和持久化调用链已随 R1/R2 收敛；不恢复 ServerApi/OfflineApi 或云 integration harness | R3 清理残留注册/孤儿文件，R6 回归 |
| M4 / V0 | 本地功能已有批次实机/测试证据；V0 严格验收仍未整体关闭 | R6 以同一候选满足全部遗留门禁 |
| M5 | 云主体删除、TUI、PATH LSP 以及本地数据/行为恢复已落地；R1/R2 已验收，R3 部分完成，R4 有明确缺口 | R3/R4：UI/URI/认证负面验证、孤儿源码、外链检查与 Windows 尾项 |
| M6 / V1 | 云 crate/协议删除、rev pin、TUI feature 隔离和历史测试删除审计已有实现或记录；不等于完整供应链/产物证明 | R5/R6：显式 git 来源、当前 inventory、累计删除集、同一候选完整验收；V1 未完成 |
| M7 / V2 | 运行时身份已提前完成；对外品牌与发布材料未完成 | V1 关闭后单独启动；不在当前收敛范围 |

### 证据与源码的对应关系

| 记录 | 可采信的范围 | 不得扩大为 |
|---|---|---|
| [R0 网络记录](phase1-acceptance/m5-m6/recovery/network-es-20260912/README.md) | 指定 debug GUI/TUI 及其 SHA256、进程树和 DNS 观测；环境/捕获能力已落实 | 最新 release/bundle 或整个产品的最终零外连证明 |
| [R1/R2 验收](phase1-acceptance/m5-m6/recovery/r1-r2-20260912/README.md) | 随 `d3fd6fdc` 归档；`candidate-source.json` / `current-checkpoint.json` 描述源码与结果；完整 presubmit 有通过日志 | 后续 `eef716cd` 已通过同一套完整检查 |
| [GUI shell 增量](phase1-acceptance/m5-m6/recovery/gui-shell-20260913/README.md) | `eef716cd` 的 12 项 focused tests、三组严格 Clippy、构建/打包及所列 GUI 场景 | WGSL、workspace nextest、doc tests 或网络全部通过 |

最新 `gui-shell-20260913/presubmit.log` 明确停在缺少 `clang-format`。这是当前应处理的
工程环境阻塞；R1/R2 曾在隔离工具目录中通过完整 presubmit，不与后来的中断矛盾。
本次仅做源码/日志复核，未在复核环境重跑 macOS 工程或实机测试。

静态核对还确认：`persistence/block_list.rs` 已改用本地终端类型；`settings_view/tab_menu.rs`
虽仍引用 `CloudModel`，但未被当前 `settings_view/mod.rs` 挂载，应按孤儿源码清理，不能再
当作已确认的编译失败。外链检查脚本、最终验收 manifest 仍不存在，`deny.toml` 仍按组织放行。

更早的依据保留：`47110e95` 的 V0 收敛、`15a202db` 的身份变更、`916039e2` 的 MCP/AWS 清理、
`f828cd6b` 的 LSP 和 `2fdeed6f` 的 TUI 本地化。历史日志保持其原始输入，不升级为当前证据。

<a id="s1"></a>
## 一、阶段关系

<a id="s1-1"></a>

```text
M0–M4：历史实现与严格验收遗留 ──┐
                              ├─ R3/R4/R5 收尾 + R6 同一候选验收 ── V1 ── M7 / V2
M5–M6：大删除与 R1/R2 批次验收 ─┘
```

R0 环境刷新仍是每次开工的前置检查，R1/R2 保留行为仍是最终回归范围；它们不是新的重复开发任务。
V0 = 本地可用与离线边界；V1 = 云控制面及依赖清理完成；V2 = 品牌、合规材料与对外发布。
许可证“阶段 2 完整 MIT 重实现”是另一条路线，不等于 V2。

<a id="s2"></a>
## 二、身份与数据边界

<a id="s2-1"></a>
### 首次真实使用

六处运行时身份已在 `15a202db` 原子落地，不再安排一次改名。权威首次使用标记仍在
[baseline/README.md](baseline/README.md)。后续任何身份/数据目录变更都必须考虑用户已有数据，
不能以清空目录代替迁移。

debug 验证可使用 `WARP_DATA_PROFILE`，release 使用隔离 HOME；从当前实现确认实际路径。
禁止照抄旧 `.warp-oss` 清理命令，更不得修改用户真实数据或旧 Warp 凭据。

<a id="m0"></a>
## M0 · 历史基线

<a id="m0-x1"></a>
<a id="m0-x2"></a>
已有工具链、测试清单、许可证和删除基线记录保留在 `baseline/` 与 `phase1-acceptance/`。
不再执行初始化 Git remote、reset、首次快照等旧开仓任务。

<a id="m0-x3"></a>
<a id="m0-x4"></a>
<a id="m0-x5"></a>
R1/R2 已实际运行 `066ec71b736fc3755e29f58f733deadbdac3d1af`，生成隔离旧 DB，验证迁移后
写入与重启；15 个历史 SQLite fixture 原字节迁至 `crates/persistence/fixtures/legacy`，
完整性检查通过。详细覆盖与限制见 R1/R2 的 `oracle-066ec71b/migration-comparison.json`
及 `fixture-integrity.json`。旧版手工标题未持久化等限制仍保留，不能据此宣布所有历史 GUI
基线已补齐。后续复用样本，不重新要求“先寻找真实旧 DB”或覆盖已有原始记录。

<a id="m1"></a>
## M1 · 网络守卫

<a id="m1-x1"></a>
<a id="m1-x2"></a>
HTTP/WebSocket loopback 守卫、无外部 DNS resolver 和边界检查已实现，保持不变。
R0 已证明本机捕获、系统 DNS 归因与短命子进程追踪可用。

<a id="m1-x3"></a>
<a id="m1-x4"></a>
门禁仍归入 11 章 §4：守卫回归 + 最终候选产物静默运行零外连/零 DNS/零重试。
R0 debug 观测不替代 release/bundle 扫描、全部场景及系统防火墙拒绝日志。
旧 `warp-oss` 构建命令仅属于历史基线，不再用于当前产品验收。

<a id="m2"></a>
## M2 · 本地产品入口

<a id="m2-x1"></a>
<a id="m2-x2"></a>
保留 `term4u`/`term4u-tui`、ConnectivityMode、local_only 和运行脚本，不新增 Offline channel。

<a id="m2-x3"></a>
<a id="m2-x4"></a>
已有批次 GUI/TUI 构建启动证据。最终仍须覆盖 default+offline_hard、local_only GUI/TUI、
release 和 bundle，以及 scheme、keyring 和数据目录身份；局部构建不替代完整矩阵。

<a id="m3"></a>
## M3 · 本地初始化

<a id="m3-x1"></a>
<a id="m3-x2"></a>
R1/R2 已接通本地持久化、初始化、终端/workspace 恢复及 notebook/workflow 调用链。
不再执行旧 ServerApi 双后端、AuthClient/ObjectClient trait 上移任务，不恢复退役 integration crate。

<a id="m3-x3"></a>
<a id="m3-x4"></a>
后续清理不能跳过恢复逻辑换取编译；按 11 章 R3/R6 检查残留入口并回归已保留的本地能力。

<a id="m4"></a>
## M4 · V0 严格验收遗留

<a id="m4-x1"></a>
<a id="m4-x2"></a>
遥测/Sentry/OTLP、更新、共享/远端、ambient/cloud environment、computer use、voice 等
主体删除已实施，不再列为新任务；Windows 残余以 R4 的实际清单为准，不能笼统称全删。

<a id="m4-x3"></a>
<a id="m4-x4"></a>
V0 关闭需要同一最终候选的构建、S1–S13、旧 DB、日志三场景、零外连/DNS/待发文件、
产物扫描、presubmit、测试清单及许可证边界证据。已全部纳入 11 章 C1–C10。
最终在 `phase1-acceptance/final/manifest.md` 建立指向 M5/M6 最终证据的索引后再关闭；
不能仅因 R1/R2 完成而提前建立 PASS 声明。

<a id="m5"></a>
## M5 · 本地功能与入口收敛

<a id="m5-x1"></a>
<a id="m5-x2"></a>
R1/R2 已完成批次验收，最新 shell 修复属于后续增量。继续执行 11 章 R3/R4：
Settings/菜单/URI/TUI 负面验证、未挂载的云孤儿源码、外链与 Windows 尾项。
blocklist 路线 A、PATH LSP、TUI 本地化、本地模型和主要云目录删除不再重复施工。

<a id="m5-x3"></a>
出口是云功能入口及调用方消失，同时本地功能/旧数据通过回归；编译没有挂载孤儿文件，
不等于源码已清理；网络守卫也不是保留无效云 UI 的理由。精确门禁见 11 章 §4/C1–C5。

<a id="m6"></a>
## M6 · 依赖和 V1 验收

<a id="m6-x1"></a>
<a id="m6-x2"></a>
执行 11 章 R5/R6：manifest/feature/显式 git 来源、当前测试清单及累计删除集、完整 macOS 验收。
R1/R2 已归档测试删除授权与逐项 ID 审计；该结果不等于最新 inventory 已重跑，也不等于
剩余本地行为覆盖完备。只增量复核，不恢复已明确退役的云套件。

<a id="m6-x3"></a>
<a id="m6-x4"></a>
**V1 达成条件**：11 章 C1–C10 全部通过，同一候选的 metadata/tree/source/lock/binary、
GUI/TUI、旧 DB、网络、macOS arm64 和完整工程证据归档到
`phase1-acceptance/m5-m6/final/manifest.md`。当前该 manifest 不存在，V1 未完成。

<a id="m7"></a>
## M7 · 后续品牌与发布（当前 Goal 不执行）

<a id="m7-x1"></a>
V1 关闭后另行安排，不以 M7 扩大本次本地化收敛范围。根 README 增加 Term4u 导航提示，
不代表对外文档/品牌交付已经完成。

<a id="m7-x2"></a>
| 后续项 | 范围 |
|---|---|
| 品牌资产 | 依 05 章 B1–B14 逐项核对，替换图标和用户可见上游品牌；运行时身份不再重复改 |
| 文档与作者 | 重写对外 README/CONTRIBUTING/SECURITY 等，更新联系方式与 authors；保留上游归属 |
| 许可证材料 | `LICENSES.md`、两个 MIT 岛的 LICENSE、生成并核对 `NOTICES.md`；不改 AGPL 代码许可证 |
| 发布准备 | 名称可用性记录、打包和签名、发布合规；首个 tag/push 另需用户授权 |

已接入的许可证守卫和 skills 下载链清理不再作为 M7 待开发项。

<a id="m7-x3"></a>
<a id="m7-x4"></a>
V2 必须在 V1 门禁基础上增加品牌扫描、归属/NOTICES、名称与发布产物验证；发布操作完成后
才能宣称 V2。完整 MIT 路线见 06 章，不在本阶段自动启动。

<a id="s3"></a>
## 三、风险登记

| 风险 | 当前处置 |
|---|---|
| RK3：删代码导致编译/死代码连锁 | R1/R2 编译收敛已有记录；区分未挂载孤儿源码与实际编译错误，后续不得增加 allow/ignore 或恢复云空壳 |
| RK4：本地功能被静默删掉 | 保留 R2 行为/旧数据测试；最新补全、clear、焦点改动进入 R6 回归，不能只靠历史 check |
| RK6：保留 git 来源许可证不明确 | `deny.toml` 组织级放行尚在；R5 完成显式来源与许可证审计，未解决不能发布 |
| RK7：身份和真实数据目录变更 | 身份已冻结；只测试副本，首次真实使用状态仍需保全 |
| RK9：网络遗漏与无效观测 | R0 已证明采集能力；R6 仍需对最终候选重新捕获，补产物和第二道防线证据 |
| RK13：local_only/unification 丢功能 | default/local_only/测试组合和 GUI/TUI 分别构建、测试，不把 workspace feature 合并视作 GUI 单独通过 |
| RK12：完整 MIT 的洁净室风险 | 留阶段 2 单独处理，不通过本轮搬运代码改变许可证 |
| 环境和证据漂移 | 最新完整 presubmit 缺 `clang-format`；补齐工具后重跑，不拼接不同候选/环境的 PASS。缺项记 INCOMPLETE |

旧 RK10（local_fs 迁移 feature 漏开）已作废；RK11（skills 资源策略）、RK14（TUI default
feature 漂移）已有实现处置，仍由 R2/R5 回归保护。旧 ServerApi 空实现工作量等风险不再适用。
仅支持 macOS，Linux 代码/配置后续清理，不把 Linux 验证列为本轮阻塞。

<a id="s3-1"></a>
### 风险复盘

批次结束将实际触发项、修复、未验证内容及证据路径写入
[baseline/risk-log.md](baseline/risk-log.md)。不得覆盖历史失败，也不得把缺环境改写成 PASS。
