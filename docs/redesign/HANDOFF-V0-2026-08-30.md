# V0 测试收敛 handoff（2026-08-31）

> 历史验证记录：以下状态和证据只描述当时的 V0 会话，不代表当前代码通过。
> 当前任务统一见 [11 · 本地化收敛施工单](11-本地化收敛施工单.md)，不恢复本文中的旧 Agent/云兼容实现。

## 当时状态

- HEAD：`1cb3da504143eb6f0406dc48f4d44997d1405288`（`checkpoint: converge V0 core packages`）。
- Worktree：测试收敛修改尚未提交；最终状态与完整 diff 以 `git status --short`、`git diff --stat`、`git diff --` 为准。
- 当前失败：无。进行中：无。Goal 阻塞：无。
- 本 session 首次仓库操作为完整读取本文旧版，随后才检查 Goal、Git、源码和工具链。

## 已完成

- 恢复并接回保留的 agent conversation/task store、agent SDK driver/artifact/retry、AI transcript 及 network logging 单元测试；按当前 API 更新测试，不新增 `ignore`/`allow`。
- 修复离线 GUI/integration 启动所需的本地 singleton、离线 execution-profile source、可选 cloud/IAP/session-sharing 依赖、integration PID getter 与离线 channel 配置。
- 删除仅覆盖已移除 preview migration、cloud websocket/Drive notebook/rule、shared-session/remote-child 及外部 GCP SSH runner 的过期测试/测试支持；未使用容器、虚拟机、远程 runner 或伪造工具链。
- 完整 Xcode、`metal`、`metallib` 当前均存在；按 Goal 明确跳过 Xcode 相关测试，未宣称其通过。

## 证据

- 最终本机真实工具链 presubmit：`/tmp/term4u-v0-session-presubmit-09.log`，`./script/presubmit` exit `0`；workspace nextest（含保留的 GUI integration）`9695 passed`，completer v2 `131 passed`，doc tests通过。
- 工具链同一日志开头：Rust `1.92.0`、cargo `1.92.0`、nextest `0.9.143`、`/opt/homebrew/bin/clang-format` `23.1.0`、`/usr/bin/xcrun`、真实 Xcode Metal 路径。
- 最近历史失败：`/tmp/term4u-v0-session-deprecated-test-cleanup.log` 中 `cargo clippy -p warp --all-targets --tests -- -D warnings` exit `101`（恢复测试引用 deprecated 字段）；已改用当前字段/default 构造并由最终 presubmit 覆盖验证。
