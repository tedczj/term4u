# V0 实施 handoff（2026-08-30）

> 对应目标：按 `docs/redesign/09-第一次实施详设-V0.md` 完成 M0–M4。
>
> **状态：未完成，按用户要求在此停止。不得把本文当作完成验收。** 当前目标的 C1/C2 仍未核验。

## 1. 停止点

- 当前 worktree 很大且未提交：`git status --short` 共 **926** 项，其中 **45** 项为未跟踪文件。
- 当前 tracked diff 统计：**881 files changed, 4,712 insertions, 173,078 deletions**。
- 不要 reset、checkout 或恢复被删除的云代码；后续必须基于当前 worktree 继续。
- 最后一次测试尝试是 Warp lib nextest，仍处于**编译失败**状态；之后已修复大部分报错，但没有重跑。
- 最新源码没有重新执行完整 format/check/clippy/nextest，因此任何较早的通过记录都不是当前最终证据。

## 2. 已完成的主要实现

### 2.1 M0–M3

- 已建立 `docs/redesign/baseline/` 基线、风险记录、测试分类与手工场景基线。
- 已新增：
  - `script/test_inventory`
  - `script/deletion_set.txt`
  - `script/check_license_boundaries`
  - `script/check_license_config_sync`
- 已实现 `offline_hard`：HTTP/SSE/WebSocket 非 loopback 拒绝、离线 DNS resolver，loopback/Unix socket 保留。
- 已实现 `ConnectivityMode`、`OfflineError`、`local_only` 与默认离线 feature 集。
- 已新增 `term4u` / `term4u-tui` 入口，删除 channel generator 与相关生成脚本/bin。
- 已实现 `ServerApiProvider` Offline 后端及 13 个 client trait 的离线实现。
- 已扩展本地初始化，注册本地终端、编辑器、设置、持久化、项目、本地 MCP 等 singleton。

### 2.2 M4a/M4b

- 遥测发送宏已 no-op；collector、event store、OTLP、Sentry 上传路径已删除。
- 自动更新、TUI 自动更新、changelog、远程实验、IAP minter、voice transcriber 等已删除/禁用。
- 隐私策略固定为本地模式，telemetry/crash/cloud storage 为只读关闭状态。

### 2.3 M4c：共享会话与远端

- 已物理删除：
  - `app/src/terminal/shared_session/`
  - 临时迁移路径 `app/src/terminal/session_sharing/`
  - `app/src/remote_server/`
  - `crates/remote_server/`
  - `crates/voice_input/`
- 当前仅保留无传输兼容层：
  - `app/src/terminal/session_sharing_disabled.rs`
  - `app/src/terminal/view/sharing_disabled.rs`
- 本地 terminal view adaptor 已去掉 shared-session network wiring。
- `app/Cargo.toml` 已移除对 `session-sharing-protocol` 的直接依赖；协议类型暂由 `warp_terminal::session_sharing_types` 重导出。`warp_terminal` 的残留 E1 按设计属于 V1/M6 清理。
- remote server、remote code review、remote file/search、录屏/computer-use 执行路径已删除或返回明确 unsupported。

### 2.4 M4d：ambient/cloud environment/computer use

以下旧路径及此前的“改名迁移路径”当前均不存在：

- `app/src/ai/ambient_agents/`
- `app/src/ai/cloud_environments/`
- `app/src/ai/cloud_agent_config/`
- `app/src/ai/cloud_agent_settings.rs`
- `app/src/ai/connected_self_hosted_workers.rs`
- `app/src/ai/agent_tasks.rs`
- `app/src/ai/agent_tasks_spawn.rs`
- `app/src/ai/agent_tasks_scheduled.rs`
- `app/src/ai/agent_tasks_telemetry.rs`
- `app/src/ai/cloud_environment_types.rs`
- `app/src/ai/agent_configuration.rs`
- `app/src/ai/agent_settings.rs`
- `app/src/ai/worker_status.rs`
- `crates/computer_use/`

保留的非运行期兼容类型已移动到现有 V1 宿主：

- `app/src/ai/agent_conversations_model/ambient_compat.rs`
- `app/src/ai/agent_conversations_model/task_types.rs`
- `app/src/cloud_object/agent_environment.rs`
- `app/src/cloud_object/scheduled_agent.rs`
- `app/src/ai/orchestration/settings.rs`
- `app/src/server/telemetry/cloud_agent_events.rs`

云 agent spawn/follow-up API 当前返回 `Cloud agents are unavailable in term4u`，不再启动云任务。GitHub OAuth、cloud environment catalog、connected worker discovery 与 scheduled-agent CLI 已移除或明确禁用。

### 2.5 本地运行的既有证据（后续改动前）

早先 TUI PTY smoke 曾通过：

- `/tmp/smoke-term4u.exp`
- `CHILD_PID=43101 EXIT_CODE=0`
- 运行期间 `lsof -nP -a -p <pid> -iTCP -iUDP` 无输出
- 双 Ctrl-C 正常退出

这些证据发生在后续大规模清理之前，必须重跑。

## 3. 最近验证状态

### 3.1 曾通过，但不是当前最终证据

以下命令曾在较早源码状态退出 0：

- `cargo test -p offline_guard`
- `cargo test -p websocket --features offline_hard`
- `cargo test -p http_client --features offline_hard`
- `cargo test -p warp_core --features offline_hard,test-util`
- `cargo check -p warp --bin term4u --no-default-features --features local_only`
- `cargo nextest run -p warp --lib --features offline_hard --no-fail-fast`
- `cargo nextest run -p warp_tui --features offline_hard --no-fail-fast`
- `script/check_license_boundaries`
- `script/check_license_config_sync`

此外，`/tmp/clippy-zero-attempt.log` 对应的

```bash
cargo clippy -p warp --bin term4u --features offline_hard -- -D warnings
```

曾退出 0，但之后又修改了 M4c/M4d、root auth、测试与依赖边界，不能作为当前证据。

### 3.2 最新失败

最新 Warp lib 测试尝试：

```bash
cargo nextest run -p warp --lib --features offline_hard --no-fail-fast
```

日志：`/tmp/nextest-warp-m4b.log`，退出码 **101**。

该次只剩 10 个编译错误；之后已修复：

- root/settings account-first 测试的多数旧引用；
- `HarnessAvailabilityModel::new_offline` 的 closure 签名；
- `TeamTesterStatus`、`TeamUpdateManager`、`UpdateManager` 的 test-only constructor；
- 多批 shared-session / observer / remote-skill / cloud-environment 测试体。

但仍确认有两处未修：

```text
app/src/pane_group/mod_tests.rs:203
app/src/workspace/view_tests.rs:224
```

两处仍调用：

```rust
PersistedWorkspace::new(vec![], HashMap::new(), None, ctx)
```

应改为当前 test constructor（通常是 `PersistedWorkspace::new_for_test`），然后重新编译确认后续错误。

## 4. 下一位接手者的第一批动作

1. **只修测试编译，不恢复已删除 API。** 先处理上述两处 `PersistedWorkspace::new`。
2. 运行：

   ```bash
   ./script/format
   cargo check -p warp --bin term4u --features offline_hard
   cargo clippy -p warp --bin term4u --features offline_hard -- -D warnings
   cargo nextest run -p warp --lib --features offline_hard --no-fail-fast
   ```

3. 对 nextest 新报错，优先删除/改写已经随云功能删除的测试；不要为了让旧测试编译而恢复 observer、shared-session network、remote skills、cloud OAuth 等 API。
4. Warp lib 通过后运行：

   ```bash
   cargo nextest run -p warp_tui --features offline_hard --no-fail-fast
   cargo check -p warp --bin term4u --no-default-features --features local_only
   cargo build --release -p warp --bin term4u --no-default-features --features local_only
   ```

5. 再按仓库要求收敛 workspace/all-target clippy；当前只验证过 package/bin 级别，不能替代 presubmit。

## 5. 尚未完成的必选项

### 5.1 测试清单

本轮删除了大量云功能测试和若干测试文件，包括 environments、scheduled/cloud agent、shared-session、observer/viewer-mode、remote skills、cloud follow-up 等测试。

必须运行：

```bash
./script/test_inventory snapshot <current-milestone>
./script/test_inventory diff <previous-milestone> <current-milestone>
```

然后逐项分类，并更新 `docs/redesign/baseline/deleted-tests.txt`。当前尚未证明“减少量精确等于 deleted-tests”。

### 5.2 当前源码验证

尚未在最新源码上完成：

- Warp lib nextest
- Warp TUI nextest
- F0/F1 release build
- 当前 TUI PTY smoke
- 60 秒零外连验证
- 当前许可证脚本
- `./script/presubmit`
- workspace/all-target clippy

### 5.3 UI 与行为核对

仍需最终扫描并手工确认以下入口全部消失或明确禁用：

- 账号/SSO/cloud auth
- Drive/团队云入口
- shared-session 分享/加入/复制链接
- cloud handoff / ambient agent / cloud environment
- 外部 OAuth/Docs/Oz 链接
- 自动更新与 changelog

兼容层仍有 shared-session/status 类型和部分 dormant UI 外壳，需确认 feature/UI 不可触达，并决定是否继续缩减 `session_sharing_disabled.rs`。

### 5.4 DB 与手工验收

尚未完成：

- 旧 SQLite DB/迁移样本加载验证；
- 文档要求的 13 个手工场景；
- 隐私只读 UI 最终截图/行为核对；
- GUI 真实启动验收。

### 5.5 C1 追踪清单

尚未生成 `docs/redesign/09-第一次实施详设-V0.md` 的逐章节追踪清单。最终必须将每个明确要求映射到：

- 代码文件/符号；
- Cargo feature/配置；
- 测试或可复现行为证据；
- 不适用项及理由。

## 6. 边界判断

- `local_only` cargo tree 仍可能包含 `cloud_object_*`。按 `docs/redesign/03` 与 `08`，这些 app/Drive/cloud-object 宿主整体删除属于 V1/M5–M6；V0 重点是云叶子物理删除、云初始化不运行、网络硬拒绝与 F1 构建通过。不要误把 V1 黑名单当成已完成证据，也不要在没有重新核对文档前随意删迁移/schema。
- `warp_terminal` 对 `session-sharing-protocol` 的 E1 仍存在，设计明确列为 V1/M6 手工剥离项；app 的直接 Cargo 边已经移除。
- 保留的 telemetry event schema 不代表发送；发送宏和传输已 no-op/删除。

## 7. 基础设施阻塞

- 当前机器仅有 `/Library/Developer/CommandLineTools`，缺少完整 Xcode 的 `metal`/`metallib`，无法给出真实 macOS GUI 全量构建/视觉验收证据。
- `/tmp/term4u-fake-toolchain/xcrun` 仅可用于编译诊断，不能作为 GUI 验收。
- 全 workspace/all-target nextest 曾耗尽约 57 GiB 磁盘；应先跑 package 级验证，再控制并发/磁盘进行全量验证。
- 当前改动未提交，云 runner 无法 checkout；跨平台 fan-out 需先形成可用 commit/branch。

## 8. 重要文件与日志

- 设计：`docs/redesign/09-第一次实施详设-V0.md`
- 删除集：`script/deletion_set.txt`
- 删除测试记录：`docs/redesign/baseline/deleted-tests.txt`
- 风险记录：`docs/redesign/baseline/risk-log.md`
- 最新失败 nextest：`/tmp/nextest-warp-m4b.log`
- 较早 clippy 通过点：`/tmp/clippy-zero-attempt.log`
- TUI 日志：`~/Library/Logs/warp-cli/term4u-tui.log`
- TUI smoke：`/tmp/smoke-term4u.exp`

## 9. 停止声明

按用户要求，本轮只落此 handoff 文档后停止。当前实现不能宣告完成，也不满足 C1/C2 的最终证据要求。
