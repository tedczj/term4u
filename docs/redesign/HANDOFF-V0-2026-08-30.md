# V0 核心 package 收敛 handoff（2026-08-30）

> 本文是有界进度检查点，不改变 Goal 合同或验收标准。权威顺序始终为：Goal 合同、当前 Git/worktree、针对当前提交的新鲜命令结果、本文。

## 1. Checkpoint 身份与边界

- 任务起点/父提交：`81f7214b71d936c0706b15bf9ad9b88338ed40e5`（`feat: checkpoint offline V0 implementation`）。
- 最终 checkpoint HEAD：**本文所在的 checkpoint commit**；用 `git log -1 --format='%H %s'` 取得不可自引用写入本文的提交哈希，提交主题为 `checkpoint: converge V0 core packages`。
- 源码 diff（不含本文）摘要：15 个文件，26 行新增、1,394 行删除；候选源码 diff SHA-256 为 `873c078473abd3b2759a7c16b5572687805e184d0de841b4f232e37769637d99`。
- 范围仅为本 Goal 的核心 package 编译、定向测试、Clippy、debug build、两处测试构造器修复，以及为这些门禁暴露出的离线/TUI 收敛问题；不把更广的 V0/V1 项目验收并入本 checkpoint。
- 最终状态要求：checkpoint 后 `git status --porcelain` 为空；以最终审计日志 `/tmp/term4u-v0-final-git-state.log` 为准。

## 2. 本 checkpoint 完成事项

- `app/src/pane_group/mod_tests.rs` 与 `app/src/workspace/view_tests.rs` 的目标初始化均改为 `PersistedWorkspace::new_for_test`。
- 删除 21 个依赖已删除 cloud、remote child、shared-session 或在线 workspace 行为的过期测试，以及只为这些测试服务的 helper/import；没有恢复对应 API。
- 修复 `app/src/tui_test_support.rs` 的 `HarnessAvailabilityModel::new_offline` singleton closure 签名。
- TUI 调用点与当前 API 对齐：
  - cloud environment refresh 保持不可用/no-op，不恢复 environment API；
  - `HarnessAvailabilityEvent` 使用当前本地 secret 事件名；
  - `local_skills_available` 调用移除已删除的 context 参数。
- `app/src/ai/custom_endpoints.rs` 不再依赖已删除的 `CloudPreferencesSyncer`；本地 legacy endpoint 迁移直接以本地设置/API-key 数据为准。
- 应用 rustfmt；`app/src/workspace/view.rs` 仅有格式化变化。
- `term4u-tui` 真实 PTY smoke 已退出 0，并观察到渲染帧；证据为 `/tmp/term4u-goal-v1-tui-live-pty-02.raw`、`/tmp/term4u-goal-v1-tui-live-pty-02.txt`。第一次 smoke 暴露的 `CloudPreferencesSyncer` singleton panic 已通过删除该依赖修复。

## 3. 最终完成证据

以下日志由 checkpoint commit 签出状态上重新执行的命令生成；每份日志末尾记录 `COMMAND_EXIT_CODE=0`，不得用本节代替日志本身：

| 门禁 | 证据日志 | 结果摘要 |
| --- | --- | --- |
| `cargo check --locked -p warp --tests --features offline_hard` | `/tmp/term4u-v0-final-c2-check-tests.log` | exit 0 |
| `./script/format --check` | `/tmp/term4u-v0-final-c3-format-check.log` | exit 0 |
| `cargo check -p warp --bin term4u --features offline_hard` | `/tmp/term4u-v0-final-c4-check-bin.log` | exit 0 |
| `cargo clippy -p warp --bin term4u --features offline_hard -- -D warnings` | `/tmp/term4u-v0-final-c5-clippy.log` | exit 0，无 Clippy warning |
| `cargo nextest run -p warp --lib --features offline_hard --no-fail-fast` | `/tmp/term4u-v0-final-c6-nextest-warp.log` | 4,497 passed，3 skipped，exit 0 |
| `cargo nextest run -p warp_tui --features offline_hard --no-fail-fast` | `/tmp/term4u-v0-final-c7-nextest-tui.log` | 989 passed，0 skipped，exit 0 |
| `cargo check -p warp --bin term4u --no-default-features --features local_only` | `/tmp/term4u-v0-final-c8-check-local-only.log` | exit 0 |
| `cargo build -p warp --bin term4u --features offline_hard` | `/tmp/term4u-v0-final-c9-debug-build.log` | exit 0；`target/debug/term4u` 为 arm64 Mach-O debug 产物 |
| 构造器、删除边界、diff 与干净状态审计 | `/tmp/term4u-v0-final-source-audit.log`、`/tmp/term4u-v0-final-git-state.log` | exit 0 |

本机只有 Command Line Tools，没有完整 Xcode 的 `metal`/`metallib`。上述 macOS 编译门禁在 PATH 前置 `/tmp/term4u-fake-toolchain/xcrun`（SHA-256 `506f56bb0f3f8f576ba9975cd6849e86e6b3aae578225a6a3173d72a40cb596e`）后运行；wrapper 仅为 build script 生成 test-only shader placeholder，其他 `xcrun` 调用委托 `/usr/bin/xcrun`。这足以证明 Rust 核心 package 收敛与链接产物生成，但不构成真实 Metal/GUI 运行验收。

## 4. Stale 结果

- 本文上一版引用的 `/tmp/nextest-warp-m4b.log`、`/tmp/clippy-zero-attempt.log`、早期 TUI smoke，以及此前所有 format/check/clippy/nextest 结果，均发生在后续源码变化前，全部 **stale**。
- 本 session 的 preflight 日志（`/tmp/term4u-goal-v1-*`）用于定位和收敛；即使其源码树与 checkpoint 内容等价，也因发生在 checkpoint commit 之前而 **stale for final acceptance**。
- 最终验收只引用第 3 节的 checkpoint 后日志与当前 Git/source 审计。

## 5. 最新失败与处置

当前失败：**无**。

最近两项历史失败均已修复：

1. `cargo nextest run -p warp_tui --features offline_hard --no-fail-fast`，exit **101**，日志 `/tmp/term4u-goal-v1-nextest-warp-tui-02.log`：缺失 TUI environment refresh、旧 harness event 名和旧 `local_skills_available(ctx)` 调用；通过删除 cloud refresh 调用并对齐现有本地 API 修复。
2. `target/debug/term4u-tui` PTY smoke，exit **101**，日志 `~/Library/Logs/warp-cli/term4u-tui.log`：`CloudPreferencesSyncer` 未注册；通过从 custom endpoint 初始化移除该 cloud singleton 依赖修复。复测 exit 0。

## 6. 进行中、阻塞与下一批动作

- 进行中：无。
- Goal 阻塞：无。
- 环境限制：缺少完整 Xcode，不能把 placeholder shader build 当成真实 macOS GUI/Metal 验收；这不改变本 Goal 的核心 package 门禁。
- 本 Goal 下一批动作：无；最终命令、checkpoint commit 和 clean-worktree 审计均已收敛。
- 若另开更广 V0/V1 session：先读本文，再用当前 Git/source 和新鲜命令复核；随后使用完整 Xcode 做 GUI/Metal 运行验收，并按 `docs/redesign/09-第一次实施详设-V0.md` 继续未纳入本 Goal 的手工场景、测试 inventory 和许可证边界工作。

## 7. 关键修改路径

- `app/src/pane_group/mod_tests.rs`
- `app/src/workspace/view_tests.rs`
- `app/src/ai/orchestration/remote_child_tests.rs`
- `app/src/terminal/input_tests.rs`
- `app/src/terminal/view/ambient_agent/model_tests.rs`
- `app/src/terminal/view_tests.rs`
- `app/src/workspaces/user_workspaces/user_workspaces_tests.rs`
- `app/src/tui_test_support.rs`
- `app/src/ai/custom_endpoints.rs`
- `crates/warp_tui/src/handoff/block.rs`
- `crates/warp_tui/src/handoff/model.rs`
- `crates/warp_tui/src/orchestration_block.rs`
- `crates/warp_tui/src/skills_menu.rs`
- `crates/warp_tui/src/terminal_session_view.rs`

## 8. Session 执行纪律记录

本 session 的第一次仓库操作是读取本文旧版；随后才执行 `git status`、`git rev-parse HEAD`、源码搜索和新鲜命令。旧 handoff 只作为定位线索，没有作为完成证据，也没有改变 Goal 验收标准。本文仅在关键测试/构建状态完成收敛并准备 checkpoint 的允许边界更新，引用日志路径而不复制大段产物。
